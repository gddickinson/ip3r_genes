`settings.local.json` holds this project's Claude Code permission
allowances. `Read(//Users/george/claude_test/piezo_genes/**)` is here on
purpose: the PIEZO project is this one's reference implementation, and
sessions are expected to read its scripts before writing the equivalent.
