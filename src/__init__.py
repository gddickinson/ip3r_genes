"""Protein Variant Finder — GUI tool for enumerating protein isoforms / orthologs.

Subpackages:
    core       — data models, search orchestrator, on-disk cache
    databases  — one client per source (NCBI, Ensembl, UniProt) behind a common base
    gui        — tkinter/ttk widgets
    utils      — exporters and species lookup helpers
"""

__version__ = "0.1.0"
