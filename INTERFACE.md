# INTERFACE.md — Module Navigation Map

> Read this **before** opening any source files. Each `.py` is < 500 lines
> and has one focused responsibility.

## Top-level

| File | Purpose |
|------|---------|
| `run.py` | CLI/GUI entry — `python run.py [--email you@example.com] [--headless --preset ip3r]`. |
| `PUBLICATION_ROADMAP.md` | **The multi-session publication plan**: session protocol, task ledger (S0–S24), emergent tasks, and the Decisions log — including the methodological rules inherited from the PIEZO project. Read at the start of every session. |
| `docs/session_briefs.md` | Detailed per-task instructions for every ledger row: goal, steps, completion criteria, outputs. |
| `docs/ip3r_background.md` | **The biology baseline.** Every statement tagged `[db]` (verified against a live database, with the query), `[lit]` (literature, verified in S0) or `[open]` (a question this project answers). Includes the ITPR/RYR hazard and the database-scale snapshot. |
| `docs/ip3r_review_2026.md` | **The literature review** — a 32-page, 137-reference, 12-figure review of the family (architecture, gating, regulation, cell physiology, paralogues, evolution, genetic models, disease, pharmacology, open questions), with §12 carrying the S0 claim audit and the figure provenance. **Generated — never hand-edit it**; edit `docs/review/*.md` and re-run `scripts/s0_review_build.py`. Cite its sources, never `ip3r_background.md`. |
| `docs/ip3r_review_2026.pdf` | The typeset review (A4, pandoc + xelatex), built by `s0_review_build.py --pdf`. |
| `docs/review/` | The review's **source**: numbered section files `00_frontmatter` … `12_methods_audit`, each under the 500-line limit. Citations are written as stable keys (`[R22]`, `[R22, R25]`) resolved against `results/s0_baseline/references.tsv`; the build renumbers them into order of first appearance. Figures are placed as a bare `![](figures/<slug>.png)` followed by a caption paragraph opening `**{fig:<slug>}.**`; `{fig:<slug>}` resolves to `Figure N` anywhere it appears, numbered in order of first appearance, so no source file carries a figure number. |
| `docs/figures/` | The review's 12 figures, png (GitHub) + pdf (the typeset build swaps the extension). **Generated** by `scripts/s0_review_figures.py`; never hand-edited, and never drawn from anything but a committed table. |
| `docs/analysis_catalogue.md` | **What the harvested data can answer** — the analysis menu behind ledger rows S15–S22, each with its inputs, method, deliverable and invalidating caveat, plus the report skeleton mapping analyses to manuscript sections. |
| `CLAUDE.md` | Session-start instructions, conventions, the family definition rule. |
| `INTERFACE.md` | (this file) navigation map. |
| `README.md` | User-facing docs + the project status board. |
| `SESSION_LOG.md` | Running notes per session: what ran, what resulted, what's next. |
| `FINDINGS.md` | **The biological story, task by task** — a dated plain-language entry appended after every completed task; unconfirmed claims marked *(pending: which task confirms it)*. |
| `roadmap.md` | Feature-development history of the app itself (inherited v1.0–v1.7 plus this project's changes). |
| `data_root.txt` | Path to the bulk-data root, read by `src/utils/data_root.py`. Edit one line to move storage. |
| `requirements.txt` | Third-party deps (`biopython`, `requests`). |
| `presets/` | Bundled JSON queries: `ip3r`, `ip3r_zebrafish`, `ip3r_discovery`, `ip3r_paralog_mine` (Compara paralog mine), plus the special-mode presets `ip3r_domain_scan` (`"mode": "domain_scan"`) and `ip3r_all` (`"mode": "exhaustive"` — full harvest + every analysis). Presets may carry `known_paralogs` and a `discovery` block of scorer-threshold overrides, honoured by both the CLI and the GUI Discovery dialog. |
| `cache/` | On-disk JSON cache (auto-created, gitignored). |
| `results/` | Committed analysis output — one directory per task. `s0_baseline/` holds the S0 database snapshot, the literature audit (`lit_claims.tsv`, `references.tsv`), the Ensembl endpoint probe and the rendered `report.md`; `s0_baseline/review_figures/` holds everything the review's figures are drawn from — InterPro domain coordinates, the 6DQN structural measurements (one subunit's Cα trace, the pore profile, `structure_meta.json`), the MAFFT conservation / identity / window tables, and the three curated tables. `benchmark_controls/` holds the S1 control benchmark: both panels as fetched, the InterPro domain hits, the MAFFT trace, the recall / specificity / bait-margin tables and the rendered `report.md`. `toolchain_manifest.txt` records the exact tool versions every later task shells out to. `census_v2/` holds S2: the enumerated census with a positive ITPR/RYR call and reason on every row, the rule audit, the unassigned profile, the sequence-level check of the call, the census-v1 delta with a verdict per missing accession, the representative table, the per-phylum core panel (the only FASTA committed — the census-scale ones are bulk) and four figures. `hmm_sweep/` holds S3's instrument: both seed sets and their MAFFT alignments, `itpr.hmm` / `ryr.hmm`, the enforced seed manifest, the reference-proteome manifest and DB stats, the calibration of profile assignment against S2's architecture call, and the sweep's assignments. `census_v3/` holds the merge: the census with **both** instruments' verdicts on every row, the novel hits, the call changes, the conflicts, the two-directional completeness tables, the jackhmmer convergence for all three seeds with its D10 verdict and per-seed model composition, four figures and the rendered `report.md`. |
| `results/s5_baits/` + `results/genome_ledger/` + `results/census_v4/` | **S5 — the genomic sweep's instrument and its 309-genome results.** `s5_baits/` holds the 38-bait panel (`baits.faa`), its rule provenance per bait, the per-candidate screen, `screen_self_test.tsv` (the screen's own negative controls, re-run on every build) and the intron calibration that sets miniprot's `-G`. `genome_ledger/` holds the pilot ledger long and wide, status counts, `locus_margins.tsv` (every found locus's family and paralog margins), the margin calibration that overturned the inherited attribution threshold (D25), `contiguity_bar.tsv` (D4's bar by class and scope), the measured S5b budget and the rendered `report.md`. `census_v4/` holds census v3 plus the sweep's 1,058 gene models with how each database holds their locus, the models the profiles declined to call, and the db-status counts. Per-genome evidence — raw miniprot GFF, rescue output, `summary.json` — is bulk and lives under `<data_root>/genome_sweep/<acc>/`, as do the census v4 model sequences. |
| `results/genome_manifest.tsv` | **S4 — the declared denominator.** One row per genome with accession, species, taxid, class, order, assembly name and level, RefSeq category, whether it carries a gene set, annotation source (D9), release date, total bp, scaffold/contig N50, size estimates, and the `reasons` it is in scope. `genome_manifest_notes.md` beside it is generated, never hand-edited. |
| `results/msa_v2/` | **S6 — the representative alignment every downstream result stands on.** The audited representative set (`representatives.tsv` — rule, cell, runner-up and the component that separated them, per tip; `length_targets.tsv` — the length ruler each group was measured against, with the tier and the record count that produced it; `unfilled_slots.tsv` naming the stage that lost each empty slot), the L-INS-i alignment and its trimAl product with `align_stats.json` recording commands, versions, runtimes and the SHA-256 of every input and output (D24), both identity matrices (covered-only and gap-counting), the per-column conservation profile, per-sequence coverage, four figures and the rendered `report.md`. S7 and S9 read `trimmed.fasta`. |
| `results/synteny/` | **S8 — orthology backed by the genomic neighbourhood.** Every ITPR and RyR locus of every swept genome with its flanking genes under two window rules (`loci.tsv`, `flanks.tsv`, `locus_sets.tsv`), the four within-cell Jaccard matrices, `pair_stats.tsv` (every pair class against its **matched random-window control**, stratified same-class / cross-class), the per-paralog flank consensus, the 2R paralogon test (`paralogon_shared.tsv` / `paralogon_summary.tsv` — the ohnologous flank families and the random-window background each had to beat), the consensus paralog caller with its calibration, its null and the threshold sweep that chose its operating point (`caller_calibration.tsv`, `caller_null.tsv`, `caller_frac_sweep.tsv`), the 131 loci it places that the sweep could not (`unplaced_loci.tsv`, each with its null tail probability), four figures and the rendered `report.md`. `synteny_stats.json` records the run's parameters, the self-tests that passed and the SHA-256 of every table. |
| `results/selection/` | **S9 — selection across the vertebrate family.** The validated CDS set and `cds_status.tsv` (route, source, candidates rejected and why, masked codons per tip), the PAL2NAL codon alignment with its trimAl product and per-paralog subsets, `tip_codes.tsv` mapping every short PAML code back to its S6 label and selection set, the pruned trees, `omega_table.tsv` / `pairwise_dnds.tsv` / `lrt_table.tsv` / `bs_restarts.tsv` / `beb_sites.tsv` / `relax_table.tsv`, `selection_stats.json` with the SHA-256 of every table, four figures and the rendered `report.md`. Raw codeml working directories live under `codeml/<job>/` and the HyPhy output under `hyphy/`. |
| `results/annotation_bugs/` | **S10 — the annotation failures, proved at the exon.** The ranking every recovered locus is scored in (`case_ranking.tsv`: annotation loss, failure mode, which eligibility rule excluded it), the E5 control per genome (`annotation_depth.tsv`), the two selected cases, and per case: every aligned exon and intron with what the annotation holds there (`case_exons.tsv`, `case_introns.tsv` — with splice dinucleotides), the annotated models over the gene and the disjoint coding blocks counted on both sides (`annotated_models.tsv`, `block_accounting.tsv`), the annotated proteins blastp-tiled onto the genome's own loci (`fragment_tiling.tsv`), the spliced CDS with its stop count against the count expected under neutrality (`reading_frame.tsv`), the junction probes and the exon-boundary control (`junction_probes.tsv`, `boundary_concordance.tsv`, `probe_summary.tsv`), the S8 flank-consensus check (`flank_consensus_check.tsv`), the build audit (`assembly_audit.tsv`), what the annotation names against what the databases serve (`family_named_models.tsv`, `database_records.tsv`), four figures and the rendered `report.md`. `annotation_bugs_stats.json` records the rules, the parameters and the SHA-256 of every table. |
| `results/structures/` | **S11 — does what the census calls an IP3 receptor fold like one, and can it be told from a ryanodine receptor by shape alone.** The AlphaFold DB probe of both the S6 representatives and the whole ITPR census (`afdb_coverage_*.tsv`, with UniProt's cross-reference beside what the API actually serves), every RCSB entity carrying a family Pfam with its family call and the rule that admitted or excluded it (`reference_candidates.tsv`), the references / state panel / negative controls with the rule that picked each (`reference_selection.tsv`, `control_candidates.tsv`, `unfilled_controls.tsv`), which taxonomic slot each predicted model fills (`model_slots.tsv`), the panel itself (`structure_manifest.tsv` — chain used, resolved residues, coverage, path and SHA-256), every TM-align pair under both normalisations (`tm_scores.tsv`), the structural family call with its margin (`tm_vs_reference.tsv`), per-domain confidence (`plddt_domains.tsv`, `plddt_summary.tsv`), four figures and the rendered `report.md`. `structures_stats.json` records the parameters, the self-tests that passed and the SHA-256 of every table. Structure files themselves — mmCIF downloads, AFDB models, the CA-trace panel and the TM-align pair cache — are bulk and live under `<data_root>/structures/s11/`. |
| `results/expression/` | **S12 — are the genes the annotation loses actually transcribed?** The scope S10 handed forward, derived from its ranking (`panel_audit.tsv`), the per-species references with their validation and **every junction tagged by whether the annotation models it** (`reference_table.tsv`, `junctions.tsv`, and `reference_validation.tsv`, which scores every reference as built, with its blocks reversed and off the wrong strand), every SRA run the search returned with why it was kept or rejected (`runs_considered.tsv`, `runs_selected.tsv`, `run_availability.tsv`), the per-run, per-locus and per-tissue counts against each locus's own reversed decoy (`expression_by_run.tsv`, `expression_by_locus.tsv`, `expression_by_tissue.tsv`), **`junction_support.tsv`** — every junction of every reference, crossed or not — the read-level form of S10's annotation loss (`annotation_gap_coverage.tsv`), binned coverage, the measured cross-mapping control (`crossmap_control.tsv` — every reference tiled with synthetic reads and mapped back), the deposit cross-check with its genomic negative control (`atlas_*.tsv`), four figures and the rendered `report.md`. `expression_stats.json` records the parameters, the tool versions, the self-test status and the SHA-256 of every table. Per-run counts and the hisat2 indexes are bulk and live under `<data_root>/expression/`. |
| `results/reconciliation/` | **S13 — where the duplications that made ITPR1/2/3 sit on the vertebrate tree.** The hand-curated, literature-calibrated species tree as an **input** (D15) — `species_tree.nwk` plus `species_tree_calibrations.tsv`, one row per named internal node with its crown age, the spread of published estimates, its **stem age** and its source — the reconciliation matrix (`reconciliation_summary.tsv`, `reconciliation_events.tsv`, `duplication_placement.tsv` with a bracket on every placement, and event-annotated Newicks per variant), the minimum-event `rooting_check.tsv` over all 112 edges of the vertebrate subtree, the loss audit against the S5 ledger (`paralog_presence.tsv`, `loss_verdicts.tsv`, `corroborated_losses.tsv`), the six cyclostome loci joined to S8's independent flank calls with the tree's own orthology pairs (`cyclostome_loci.tsv`) and the long-branch check behind them (`branch_lengths.tsv`), four figures and the rendered `report.md`. `reconciliation_stats.json` records the parameters, the self-test status and the SHA-256 of every table. |
| `results/loss_dynamics/` | **S15a — the loss instrument: what counts as a loss, and whether anything in 309 vertebrate genomes reaches it.** `character_matrix.tsv` is the deliverable — one row per genome × paralog cell with the state, the rule that assigned it and the number that rule fired on, plus the second (synteny) evidence axis beside it — with `character_matrix_wide.tsv`, `state_counts.tsv`, `implied_copies.tsv` (the additive per-genome gene-equivalent count, and D4's contiguity flag beside it) and `loss_candidates.tsv`, the near-miss list that names the rule that stopped each cell and is what S15b's sensitivity matrix is read off. The instrument: `reconstruction.tsv` (every candidate reference reassembled across contigs, in three attribution scopes, with its tiling geometry and its subject-side exonic length) and `recon_calibration.tsv` (the measured bar, both metrics, with Youden J, the gap's two edges and the `co_trace` population that made the first calibration fail — D46). The brief's synteny step, answered by a number: `synteny_reach.tsv` / `synteny_reach_summary.tsv` (per region: genes on its own contig, observed contig extent, informative keys, and *why* the caller could not act), `caller_accuracy_by_keys.tsv` (S8's own calibration re-binned onto the axis the traces live on) and `synteny_calls.tsv` (the eight regions it did reach). The ORF screen: `integrity_loci.tsv` (every placed locus, its lesion density per kilo-residue and a one-sided verdict), `integrity_covariates.tsv` (the three confounders — identity is the one that matters, D47), `integrity_pairs.tsv` and `integrity_tests.tsv` (the paired within-genome sign tests, run all-pairs and identity-matched, BH-corrected across four). The tree as an input: `species_tree_309.nwk`, `tree_placement.tsv`, `tree_polytomies.tsv` and `tree_vs_s13.tsv`. Four figures and the rendered `report.md`; `loss_dynamics_stats.json` records the rules, the parameters, the self-test status and the SHA-256 of every table. |
| `manuscript/` | The submission package, built by `scripts/s14_assemble.py` in S14a. See `manuscript/README.md`; nothing here is hand-edited except the numbered section files. |

## `scripts/` — roadmap-session tooling (not part of the app)

Ported and ready to use from session one:

| File | Purpose |
|------|---------|
| `dashboard.py` | **Project dashboard generator** (stdlib-only) → `dashboard.html` (gitignored): overall + per-task progress parsed from the roadmap ledger, a live panel for the in-progress task driven by `results/session_live.json`, key figures (explicit list first, then auto-discovered from `results/*/figures/*.png`, base64-embedded), and FINDINGS.md rendered. Meta-refreshes every 30 s. Session protocol step 0: `--open` plus a background `--watch`. |
| `figstyle.py` | **The one figure style every publication figure goes through.** Page geometry imported from `s14_lib`, so a figure is drawn at the width it is placed at. The validated palette: `PARALOG` (the fixed ITPR1/2/3 trio), `GROUP` (adds the non-vertebrate grade and the RyR outgroup in accent violet), `STATUS`/`QUALITY` (the ordered blue→grey→red evidence scales), `CLINICAL` (reserved status colours). `panel()`, `despine()`, `hgrid()`, and `save()`, which writes png+pdf at 400 dpi and **raises** rather than writing a figure whose tight bbox runs off-canvas or whose text needs a glyph the font lacks. |
| `s14_lib.py` | Manuscript-assembly shared helpers: page geometry, the section order, the main / Extended Data / Supplementary figure maps (the *plan* — source paths are where the tasks write their figures), the deposit rules, and stdlib TSV/JSON/SHA-256/PNG-size helpers. |
| `s14_figures.py` | Copies every committed figure into `manuscript/figures/` under its publication number, in every format the analysis produced. Nothing is re-plotted, so a manuscript figure cannot differ from the one in its results directory; files not in the manifest are deleted on every build. |
| `s14_claims.py` | **The manuscript's drift guard.** Every load-bearing number is declared with its source table and the op that recovers it (`count` / `cell` / `nunique` / `sum` / `json` / `grep`, with `col__startswith` and `col__in` filters). Ships with an empty `CLAIMS` list and a worked example of each op. |
| `s14_deposit.py` | Walks the deposited tree → `deposit_manifest.tsv` with size + SHA-256 per file, and `deposit_notes.md` listing the bulk classes deliberately excluded **with the command that regenerates each**. |
| `s14_pdf.py` | The typeset PDF: builds a pandoc markdown with each figure placed above its own legend, then xelatex via pandoc. Three traps are handled in code and commented there. |
| `s14_assemble.py` | The driver — ordered stages `figures → claims → stitch → deposit`; `--only` / `--from` / `--list`; non-zero exit on a missing figure, a missing section or a failed claim. |
| `s0_db_snapshot.py` | **S0 step 4** — re-derives every `[db]` number in `docs/ip3r_background.md` (InterPro signature + taxonomy counts, the UniProt reference and sister-family panel, per-protein Pfam architecture, the zebrafish PF08709 query) into `results/s0_baseline/*.tsv`. Re-run it whenever a `[db]` number is about to be quoted. |
| `s0_gene_structure.py` | **S0 step 4** — exon counts, genomic spans and cytobands for ITPR1/2/3 from Ensembl `lookup/symbol?expand=1` (deliberately *not* `xrefs/symbol`, which stalls for `homo_sapiens` — see the S0 report). This is what falsified the "~58–60 exons, hundreds of kb" claim. |
| `s0_report.py` | **S0** — renders `results/s0_baseline/report.md` purely from the committed tables (D13). Nothing in that report is hand-written. The claim-audit reference count comes from `lit_claims.tsv`, not from the size of `references.tsv`, because that table also backs the review. |
| `s0_review_build.py` | **Assembles `docs/ip3r_review_2026.md` from `docs/review/*.md`** and renders the bibliography from `references.tsv`; `--pdf` typesets it via pandoc + xelatex, `--check` validates without writing. A cited key with no reference row is a build error, so the text and the bibliography cannot drift. It also numbers the figures in order of first appearance and resolves `{fig:<slug>}` — a reference to a figure that is not placed exits 3, a placed figure whose png/pdf is missing exits 4. Rewrites `<sub>`/`<sup>` into pandoc syntax, swaps each figure's png for its vector pdf inside a `center` block (so figures stay where the text puts them), and drops the duplicate H1. |
| `s0_figdata_domains.py` | **Review-figure data** — per-protein Pfam *coordinates* from InterPro for the reference panel, the sister panel and the non-vertebrate grade → `review_figures/domain_coords.tsv`. `pfam_architecture.tsv` says which signatures a protein carries; this says where they sit, which is what lets the ITPR/RyR comparison be drawn rather than asserted. |
| `s0_figdata_structure.py` | **Review-figure data** — measures the channel from PDB **6DQN** (human IP₃R3, IP₃-bound, 3.33 Å, the review's [R24]): four-fold axis, C4 residual, pore-radius profile, the residues lining the filter and the gate, the IP₃ contacts, and the ligand→gate geometry. The 14 MB mmCIF goes to a `--work-dir` **outside the repo** (refuses a path inside it); what is committed is one C4-symmetric subunit's Cα trace, the pore profile and a measurements JSON. Self-checking: it exits non-zero unless the narrowest luminal point lands on the GGGVGD filter motif, which nothing in the geometry knows about. |
| `s0_figdata_align.py` | **Review-figure data** — two MAFFT alignments of the committed control panels: the 25-sequence family alignment (→ per-column conservation on human ITPR1 numbering) and an 8-sequence ITPR+RyR superfamily alignment (→ all-pairs identity under both metrics, and the zoom windows). The windows are anchored on the sites *measured* by `s0_figdata_structure.py`, not on a residue list from a paper. Records MAFFT's version and return code, because S1 established that a silent MAFFT failure falls back to a star alignment. |
| `s0_figdata_curated.py` | **Review-figure data** — the three hand-curated tables (milestones, regulators, disease sites) that no database returns. Every row carries the review's stable citation keys and the script **fails** if one has no reference row; no milestone year is typed in (each is read from `references.tsv`); and variant positions are recorded as `point` / `domain` / `gene` by how far the cited source actually localises them. |
| `s0_fig_lib.py` | Shared figure helpers: table loading, the domain palette (which encodes *shared with RyR* / *generic pore* / *RyR-only* rather than domain identity for its own sake), the domain-track primitive, `W_REVIEW` (the review's own text width — `figstyle.W_FULL` is the manuscript's), and `provenance()`, the corner tag that marks every figure **measured**, **computed**, **schematic** or **curated**. |
| `s0_figs_structure.py`, `s0_figs_sequence.py`, `s0_figs_genomics.py`, `s0_figs_concepts.py`, `s0_figs_clinical.py` | The 12 review figures, three or four to a module. Each renders **only** from committed tables (D13 applied to figures). |
| `s0_review_figures.py` | The figure driver — `--list`, `--only <slug>` (repeatable), else all 12; maps each slug to the section it illustrates. |
| `s1_toolchain.py` | **S1 step 1** — probes every external binary (PATH first, then the recorded conda env) and every Python package the pipeline imports, and writes `results/toolchain_manifest.txt` + `benchmark_controls/toolchain.tsv`. A tool that only exists inside the env is recorded *as* env-resident rather than silently passing as a PATH tool. |
| `s1_panels.py` | **S1 ground truth** — the two control panels and their live UniProt fetch (Swiss-Prot preferred, longest as tie-break), cached to `panel_positives.json` / `panel_decoys.json` + FASTA so the benchmark reruns offline. Positives: ITPR1/2/3 across a vertebrate panel plus the invertebrate / non-metazoan single-Itpr grade. Decoys: RYR1/2/3 across species (the sharp decoy — they carry every diagnostic Pfam), the MIR-domain sharers POMT1/2, in-band channels and non-channels, and out-of-band giants. |
| `s1_lib.py` | S1 helpers: `MafftTracer` (wraps `subprocess.run` to prove MAFFT really ran, with its return code), candidate lookup by label or dedup key, the component decoder that reads which scorer components fired back out of the evidence text, and `bait_margins()` — the ITPR-vs-RyR labelled-bait margin that operationalises D14/D7. |
| `s1_benchmark.py` | **The S1 driver** — mirrors the production wiring (live InterPro domain lookup, `analyse(use_mafft=True)`, MSA-signature fallback, `discover_novel_paralogs`) across a baseline run plus one hold-out per paralog, and writes tables only. Also measures every margin under both identity metrics (full-alignment and `covered_only=True`) and diagnoses each recall failure. |
| `s1_report.py` | **S1** — renders `results/benchmark_controls/report.md` purely from the committed tables (D13). |
| `s2_lib.py` | S2 shared helpers: the seed / context / RyR signature lists (and *why* MIR is not a seed), the two-host InterPro fetch with backoff, `signature_names()` (Pfam IDs resolved live and cached so no report hard-codes what a signature means), TSV I/O and the dashboard live-panel writer. |
| `s2_interpro.py` | **S2 step 1** — walks each seed signature to exhaustion, archiving every raw page under the data root and then re-parsing the census *out of the archive*, so the parse is reproducible offline (`--parse-only`). Resumable: the cursor is persisted after every page, and a cursor the API later rejects restarts that signature rather than silently resuming onto a shifted result set. Records InterPro's advertised `count` and UniProt's independent count beside the number actually served — which is how S2 found the advertised count is 168 short for PF08709. |
| `s2_uniprot.py` | **S2 step 2** — one streamed UniProt query returns the whole seeded space *with its full Pfam architecture* (`xref_pfam`), lineage and length, turning ~15,000 per-protein InterPro lookups into a single request. Parses UniProt's ranked lineage into rank columns plus a coarse `group` bucket. |
| `s2_call.py` | **S2 step 3** — the positive ITPR-or-RYR call on every record, on architecture (D14): RYR on any RyR-specific signature, ITPR on the *complete* IP3-receptor architecture with none of them, length as support only. Writes `census_v2.tsv`, the call summary, the conflicts, the unassigned profile, and `rule_audit.tsv` — which scores the architecture rule against gene symbols the rule never sees. |
| `s2_sequences.py` | **S2 step 4** — the seeded-space FASTA and the full representative set (both bulk, both under the data root), plus the committed per-phylum **core panel**. `load_fasta()` handles UniProt's `sp|ACC|NAME` and this project's `ACC|GENE|Species|PARALOG` headers explicitly, because reading the wrong field fails silently. |
| `s2_verify.py` | **S2 step 5** — checks the annotation-derived call against sequence: D14's labelled-bait identity margin over the core panel, under both identity metrics, scored separately inside and outside D7's no-call band (read off `DiscoveryConfig`'s own default, not retyped). |
| `s2_delta.py` | **S2 step 6** — census v1 (the app's own search bundles + S1's panels) vs census v2, by lineage, with a **verdict on every v1 accession absent from v2**; the unexplained residue is resolved against UniProt's own Pfam list rather than assumed. |
| `s2_figures.py` | The four S2 figures, rendered only from the committed census tables (D13, D19). |
| `s2_report.py` | **S2** — renders `results/census_v2/report.md` purely from the committed tables (D13). |
| `s3_hmm_lib.py` | S3 shared helpers: FASTA IO (including a streaming reader for the 8.8 GB sweep DB), UniProt FASTA-header parsing, `parse_domtblout()` / `best_hits_by_target()` (which merges domain rows into per-target coverage and takes the **full-sequence** bit score, because that is what the profile margin is measured on), and `parse_jackhmmer_log()` — which recovers each round's *included target list* as well as its counts, since D10's kill criterion has to be evaluated on evidence rather than on a total. |
| `s3_seed_spec.py` | **The committed seed manifests for both profiles**, with the five selection rules written out (census-derived labels, one seed per species per paralog, complete architecture unless the note says why not, the length band as support, the two sets disjoint and equally deep). Also `JACKHMMER_SEEDS` — the three single-sequence seeds of the convergence runs. Every deliberate exception carries its reason in the row, including Q9NA13 (*Dictyostelium* iplA, 2/5 signatures, in the ITPR set precisely so the profile can find its relatives). |
| `s3_build_seed.py` | **S3 step 1** — builds `itpr.hmm` **and** `ryr.hmm` from those manifests: pull each seed out of S2's archived seeded-space FASTA, MAFFT L-INS-i, hmmbuild. Every selection rule is *enforced*, not trusted — a seed whose census call, architecture count or length has drifted since the manifest was written aborts the build (it caught one mis-transcribed architecture count on the first run). MAFFT runs **single-threaded** (D24): at `--thread -1` it is not reproducible, and the same seeds gave profiles of 4,933 and 4,908 match states on consecutive runs. Its return code and a ragged-alignment check are hard failures (S1: a silent MAFFT failure degrades to a star alignment with no other symptom), and the SHA-256 of every seed set, alignment and profile goes into `seed_build_stats.json` so a rebuild that drifts is visible in the data. |
| `s3_assign.py` | **D14 at HMM scale**: score against both profiles, assign to the winner, and only when it beats the loser by more than `REL_MARGIN` (0.10 — D7's number) of its own score and clears `MIN_SCORE` bits. The margin is **relative** because bit scores scale with alignable length, so a fixed gap would call every full-length protein and no fragment. `audit_against()` scores the assignment against a call it never sees. Nothing here reads a gene symbol, a protein name or a length. |
| `s3_kill.py` | **D10's coded kill criterion.** Three rules evaluated per jackhmmer round on that round's own inclusion list: **K1** sister-family contamination over 5 % (the family-specific rule — an ITPR-seeded run drifting across the shared architecture into the RyRs is *the* failure mode here), **K2** a round including more than 10× the previous one, **K3** the 10-round ceiling without convergence. Rounds from the first firing on are reported but excluded from the merge. |
| `s3_fetch_proteomes.py` | **S3 step 2** — UniProt reference proteomes for a taxon group → one gzip per proteome under the data root plus a concatenated sweep DB, resumable on gzip validity. Canonical sets only: isoform sets add isoforms of genes already counted, and the census counts genes. The five groups partition Eukaryota so S20 reuses the code path without sweeping anything twice. |
| `s3_calibrate.py` | **S3 — measuring the instrument before using it.** Runs both profiles over S2's whole archived seeded space and scores the profile assignment against S2's architecture call, which read different evidence entirely. Reports agreement twice, with and without the seed accessions, because the seeds are in that set by construction. |
| `s3_run_sweep.py` | **S3 steps 2–3 driver** — both profiles' `hmmsearch` over the 8.8 GB sweep DB then the margin assignment, and the `jackhmmer` runs to convergence, per-tag so separate processes can run one seed each in parallel. `--parse-only` re-derives a run's convergence tables from its **archived log** without searching again (the S2 discipline applied to HMMER): the per-round *included target lists* D10 is evaluated on exist only in the log, so being able to re-parse them offline is what makes the kill criterion re-checkable. A re-parse carries forward the recorded runtime rather than erasing it. Writes the dashboard live panel. |
| `s3_census_v3.py` | **S3 step 4** — merges the two instruments into `results/census_v3/`: every v2 record gains its profile verdict, novel sweep hits are appended, and the merged call is a stated rule over both (D23: agree → high; one speaks → that one; disagree → `conflict`, kept and reported; neither → `unassigned`). Applies D22's gate at the merge so module-only hits are counted but not enrolled, keeping the ones carrying a family gene symbol as annotation leads. Measures completeness the expensive way round — each v2 ITPR record the sweep failed to find is **looked up in the 8.8 GB database itself**, one streaming pass, so "not in the reference proteome" is told apart from "in it and missed". Also profiles what each jackhmmer model is actually built from, by profile call × evidence class. |
| `s3_figures.py` | The four S3 figures, from the committed S3 tables only (D13, D19): profile separation with the no-call band drawn rather than described, the jackhmmer completeness curve beside its sister-contamination trace, which instrument calls each record, and per-proteome copy number against annotation depth. |
| `s3_report.py` | **S3** — renders `results/census_v3/report.md` purely from the committed tables (D13). |
| `s3_report_d10.py` | Section 4 of that report — jackhmmer, the D10 verdict per seed with its wall clock, and **what each iterated model is actually built from**. Split out of `s3_report.py` to keep both under 500 lines; it takes the caller's table loader rather than re-importing one, so the two modules cannot read the tables differently. The composition paragraph is computed per evidence class, because with three seeds the family categories agree exactly and the non-family ones do not. |
| `s4_manifest_lib.py` | S4 shared logic: `datasets` JSONL parsing, the assembly rank function (annotated > RefSeq > level > scaffold N50, D9), one-best-per-order selection, and **`derive_margins()`** — the margin species computed from the committed census tables by four stated rules (`zero_hit_proteome`, `missing_paralog` at fewer than 3 paralogs, `fragment_only` below `family.MIN_LENGTH_AA`, `anchor`) rather than hand-listed, so the denominator rebuilds from the census. Also `datasets_bin()`, since the CLI is env-resident and not on PATH. No network calls. |
| `s4_build_manifest.py` | **S4 driver** — cached `datasets` dumps → `results/genome_manifest.tsv` + `genome_manifest_notes.md`; 161 orders ∪ 169 margin species = 309 genomes. Every call is archived under `<data_root>/raw_api/ncbi_datasets/`, so a rerun is offline unless `--refresh-assemblies`. Resolves each margin species to its best assembly with the **same** rank function the order reps use — a last-wins taxid lookup put one species in twice under two accessions. |
| `s4_notes.py` | Renders `genome_manifest_notes.md` from the manifest rows (D13 applied to the scope document): the scope rule, the margin rule table with its thresholds and counts, class breakdowns, the D9 annotation-source split, the storage estimate against measured free space, and the assemblies over 10 Gbp. |
| `s5_intron_calibration.py` | **S5 step 0 — the threshold nobody can guess.** miniprot's `-G` default (200 kb) is not a performance knob: a gene whose largest intron exceeds it is *split*, and a split ITPR reads out of the ledger as `fragment`. Measures the largest single intron of ITPR1/2/3 and RYR1/2/3 across an 11-species Ensembl panel with each assembly's size, and derives the scaling rule the sweep uses. Result: **no ITPR gene in the panel has an intron over 200 kb** (widest 152 kb, human ITPR1) while the RyR control exceeds it twice (human RYR2 228 kb) — so the default is adequate for the deliverable and marginal for the control. → `results/s5_baits/intron_calibration.{tsv,json}` |
| `s5_bait_spec.py` | **The seven rules that choose the S5 bait panel**, written out and enforced rather than described: census-derived paralog labels (R1), full length *and* complete architecture (R2), one bait per paralog per clade band and two in the bands that dominate the scope (R3), the profile chimera screen (R4), the RyR baits as a presence control (R5), S3's seeds reused not re-derived (R6), and the panel scoped to this sweep's vertebrate genomes (R7). Also `band_of()`, which resolves a census row's clade band class → order — UniProt's ranked lineage has no class rank for the coelacanth, and a class-only lookup silently dropped the Latimeria seed from the first build. |
| `s5_build_baits.py` | **S5 step 1 driver** — derives the panel from `census_v3.tsv` by those rules, pulls sequences from committed/archived files only (no network), screens every candidate *and every incumbent S3 seed*, and writes the panel with its full provenance. 38 baits (30 ITPR + 8 RyR control), 121,294 residues, 25 reused seeds + 13 additions. Six slots stay unfilled — ITPR1/ITPR2 in chondrichthyans, ITPR2 in the coelacanth grade, all three in cyclostomes — because no labelled full-length record exists there; the `vertebrate_basal` baits cover those genomes unlabelled and S7 settles the paralog. → `results/s5_baits/` |
| `s5_bait_screen.py` | **D5's chimera screen, run with this project's own profiles.** Scores each candidate against `itpr.hmm` and `ryr.hmm` under S3's relative margin (family, positively — D14) and requires the winning profile's envelope to cover the bait with no large unaligned interior (shape). `self_test()` is a negative control for the screen itself, run on **every** build: three synthetic failures are constructed from real panel baits and each must be rejected by the rule responsible. It earned its place on first run by rejecting a badly-aimed test — a truncation aligns 100 % of *itself* to the profile in one segment, so it is the length band's job, not the envelope's. |
| `s5_sweep_lib.py` | The alignment layer: bait loading, miniprot execution (with whole-contig chunking for assemblies too large to index in one pass), GFF/`##STA` parsing, and locus clustering. `max_intron_for()` reads the committed calibration rather than a retyped constant, and `intron_rule_capped()` flags the genomes where the tractability cap binds. `Locus.family_margin()` and `Locus.paralog_margin()` record, per locus, how far the winning family and the winning paralog beat their runners-up — the numbers that turn a best-hit assignment into a call with a margin. |
| `s5_classify.py` | **Where D14 lives.** The family call comes first and is a positive test on alignment score: a locus won by the RyR baits is never offered to an ITPR paralog cell, and only inside the winning family is the paralog question asked. Also the CDS-footprint test that decides whether a locus is already an annotated gene (scored against the alignment's exons, not its span, so a passenger gene inside a 152 kb ITPR intron is not mistaken for the ITPR annotation), and the `vertebrate_basal` offer that keeps a real shark or lamprey gene from reading as absent because the species' nearest bait outranks every labelled one. |
| `s5_rescue.py` | Whole-genome tblastn rescue for cells miniprot left empty, HSP clustering into regions, and attribution of each region against the **full** panel — family first (a piece of a RyR must never be reported as the remnant of a missing ITPR), then paralog. Rescue baits are derived from the committed panel, spread across clade bands, so they cannot drift from `bait_manifest.tsv`. |
| `s5_run_sweep.py` | **The S5 driver** — per genome: fetch, annotation, miniprot, classify the three paralogs **and the RyR control**, rescue the empty cells, and write `summary.json` under the data root. Resumable per genome, shardable, `--delete-after` for fetch → search → purge operation. A genome whose RyR control does not fire is called out on its own line: that is an assembly or pipeline problem, not a biological result. |
| `s5_ledger.py` | Assembles every completed `summary.json` into the task's deliverable tables (D13 — nothing is recomputed, the summaries carry the calls): the long ledger one row per genome × class, the wide one row per genome, status tallies overall and by class, the RyR control failures, and `locus_margins.tsv` — every found locus's family and paralog margins with whether the annotation agrees. → `results/genome_ledger/` |
| `s5_calibrate_margin.py` | **Asks whether the inherited attribution margin transfers.** `s5_rescue.ATTRIBUTION_REL_MARGIN` arrived as the PIEZO port's 1.5× bit-score ratio (0.333), from a family whose paralogs are 40–50 % identical where these are 61–68 %. Measures the paralog separation actually available at loci whose identity the assembly's own annotation establishes — an **upper bound** on what a rescue fragment can achieve, which makes it decisive in one direction: a threshold complete evidence fails cannot be met by a fragment. → `results/genome_ledger/margin_calibration.{tsv,json}` |
| `s5_test_chunked.py` | **Equivalence test for the chunked miniprot path** — the code the two giant assemblies depend on and that nothing else exercises. Runs a genome both ways and requires the same loci, baits, coverage and cell statuses, with boundaries inside a stated tolerance (terminal exons jitter a few bp at a chunk edge; a real coordinate-offset bug moves loci by megabases). Also measures peak RSS per Gbp. On first run it proved the mRNA-ID collision is real (167 rows, 60 distinct IDs over 6 chunks — the guard in `parse_miniprot_gff` is load-bearing), caught its own broken high-water-mark measurement, and produced the number that cut `CHUNK_BP` from the ported 4 Gbp (~25 GB RSS) to 2.5 Gbp (~20 GB). |
| `s5_test_resume.py` | **The two properties a resumable sweep must have**, since the driver skips any genome with a `.sweep.done` marker and reuses any non-empty `miniprot.gff`. (1) A failed or killed run leaves nothing reusable — proved with a test double that writes output then exits non-zero. (2) Re-running a genome reproduces its summary, `--full` extending that through miniprot itself. The second is D24's question asked of a second tool: **miniprot is reproducible at a fixed thread count**, where MAFFT is not at `--thread -1`. Volatile fields (timings, `run_at`) are excluded by an explicit list, not a wildcard, so a field that starts varying cannot hide. |
| `s5_census_v4.py` | **S5b step 3** — merges the sweep's genomic gene models into census v3, scoring each against `itpr.hmm`/`ryr.hmm` so a v4 row rests on the same instrument that called v3 (D23). Records **how** a database holds each locus, not merely whether: `genome_only`, `annotated_unnamed`, `annotated_other_paralog`, `annotated_named`. The label describes the observation rather than a verdict — one instrument does not overturn a public annotation — and `sibling_locus_for_annot_paralog` records what makes a disagreement coherent. |
| `s5_figures.py` | The four S5 ledger figures, from committed tables only (D13, D19). The contiguity panel **bins rather than smooths**: a sliding window straddling D4's bar reports a contiguity no genome in it has, drawing the curve through the very threshold the panel exists to show. |
| `s5_calibration.py` | **The three S5 thresholds that are measurements, not settings**, read back out of `results/s5_baits/intron_calibration.{json,tsv}` rather than written into the code — the discipline D13 applies to reports, applied to constants. `max_intron_for()` (a too-small `-G` splits a gene and a split ITPR reads as `fragment`), `intron_rule_capped()` (the cap is tractability, not biology, so the genomes it binds on are flagged) and `itpr_span_stats()` / `spans_a_gene()` (D4's contiguity bar: an assembly whose contigs are shorter than the gene cannot carry it). |
| `s5_budget.py` | Projects the full sweep's cost from the pilot's rates, measured on **clean recomputes** — a cached `miniprot.gff` makes a rerun look free, which is how a budget gets written from a meaningless number. Also writes `contiguity_bar.tsv`, the D4 bar broken out by class and by scope, because *which* genomes fail it is the result: 66 % of Aves against 11 % of Actinopteri, 68 % of margin species against 12 % of order reps. |
| `s5_report.py` | **S5** — renders `results/genome_ledger/report.md` purely from the committed tables (D13). Detects the run's scale from `ledger_stats.json` and titles itself accordingly; the instrument sections live here, the results sections in `s5_report_results.py`. |
| `s5_report_results.py` | The results half of that report, split to keep both under 500 lines and taking the caller's loader and formatter so the two halves cannot read the tables differently (the `s3_report.py` / `s3_report_d10.py` pattern). **Every headline is chosen by the data, not asserted**: S5a's findings came from six genomes and are hypotheses at 309, so each section computes its statistic, compares it against the pilot's recorded value in `PILOT`, and renders `confirmed` / `not confirmed` / `contradicted` / `underpowered` — printing the pilot's number beside the new one either way. A generator written to narrate the pilot's answers would print them whatever the sweep said, which is how a pipeline launders an assumption into a result. The annotation-quality section **holds contiguity constant**, and that control is load-bearing: uncontrolled, ITPR3 looked 39 points better annotated than ITPR2; above the bar the three are within 6 points, so the apparent annotation gap was assembly quality. |
| `s6_rep_spec.py` | **The eight rules that choose the S6 representative set** (D8: per clade *and* per kingdom, explicit rules, never longest-per-species — a longest-first pick reliably selects chimeric gene models). What is specific to this family: **no rule ranks or filters on sequence identity**, because S23 (D29) retired identity as a call gate outside the vertebrates; **a paralog label is vertebrate-only** in `group_of()`, because ITPR1/2/3 are a 2R product and a protist whose UniProt name reads "receptor type 2" carries that number by annotation transfer, so colouring such a tip as a paralog would assert what S7 is being run to test; and **R5 is gated on S20's per-record plant/fungal verdict**, so a `contaminant_suspect` 95-100 % identical to one animal cannot become the plant branch's tip. `rank_key()` is lexicographic rather than a weighted sum precisely so the audit can name the component that decided each pick. |
| `s6_select_reps.py` | **S6 step 1** — census v6 → `results/msa_v2/representatives.{tsv,fasta}`, applying those rules and writing the audit: rule, cell, runner-up, and which component separated them. Length targets are **measured, not typed, and never borrowed** — each group's own median over its non-fragment records carrying at least four of the five signatures, because the family's size varies far more outside the vertebrates than in and one target would score the plant and ciliate grades against a vertebrate ruler. The bar is four rather than five, and the record floor three rather than five, because the strict version starved two groups into borrowing the *global* median: Viridiplantae had 2 qualifying records and Amoebozoa 1, so both were scored at 2,694 aa — a metazoan number — and in the plant grade that decided a tip (**D35**). At four of five the well-populated groups barely move (Vertebrata 2,671 → 2,671, SAR 2,973 → 2,978) and the starved ones gain a real ruler (Viridiplantae 13 records → 3,182 aa). `length_targets.tsv` commits the tier and record count behind every target, so a number computed from four records is visible as such. R6 runs before the phylum rules (the expansions are the scarcer evidence) and one genome per phylum, so four copy-number slots are not spent on two sponges. |
| `s6_lib.py` | S6 shared plumbing: the census loader that normalises the one column S23 left in two spellings (`Viridiplantae` / `viridiplantae` — counted naively that is two groups); `SequenceStore`, which resolves an accession across the four small archives and streams the 38 GB of proteome DBs only for what they miss, caching the answers so a re-run is offline; `binomial()`, because UniProt appends common names and the genome sweeps do not, so a raw-string dedup key treated *Prymnesium parvum* as two species; and `pick_diverse()`, whose keys are **nested** — with `(phylum, genus)` wave 1 takes one per phylum *and* one per genus, which is what stops five SAR slots going to three oomycetes. A key level with only **one distinct value** among the candidates is skipped, because such a level carries no diversity information and can only throttle: every Viridiplantae candidate is Chlorophyta, so the phylum prefix admitted one row per wave and by wave 3 the genus quota had loosened enough to pass a *second* Chlamydomonas while six other genera waited (**D35**). |
| `s6_msa.py` | **S6 step 2** — MAFFT L-INS-i then trimAl `-automated1`, then the two identity matrices, the conservation profile and per-sequence coverage. **`--thread 1`, by decision (D24)**: L-INS-i at `--thread -1` is not reproducible on this machine, and everything downstream is built from this file; the SHA-256 of input and output go into `align_stats.json`. A ragged alignment is a hard failure, not a warning — S1 established that a silent MAFFT failure degrades to a star alignment with no other symptom. |
| `s6_figures.py` | The four S6 figures, from committed S6 tables only (D13, D19). The conservation figure maps human ITPR1's Pfam architecture onto the profile **through the alignment** — by walking the human row and counting ungapped positions — rather than scaling residue coordinates onto column coordinates. |
| `s6_test_selection.py` | **Negative controls for the selection rules, run on every build** (`s5_bait_screen.self_test()`'s pattern): six constructed cases each rejected by the rule responsible — a fragment, a RyR-length record offered as an ITPR, an S20 `contaminant_suspect`, a name-transferred "type 2" protist, one species under two spellings, a score-ranked single-clade pool — plus the two properties the step before the aligner has to have for D24 to mean anything: **determinism** (two selections give the same set, order and audit) and **Newick-legal unique tip labels**. |
| `s6_report.py` + `s6_report_results.py` | **S6** — renders `results/msa_v2/report.md` purely from the committed tables (D13); scope and instrument here, results in the second half (the `s3_report.py` / `s3_report_d10.py` split). **Headlines chosen by the data**: S1 measured ITPR-to-RyR covered identity at 0.249 and within-family at 0.828, so §4.1 computes both on this alignment and renders `confirmed` / `contradicted` against them. The sister question is deliberately reported as a **preview, not an answer** — the alignment can rank the three between-paralog identities but a mean identity is not a phylogenetic estimate, and S7's AU test is what settles it. |
| `s7_lib.py` | S7 shared tree handling, stdlib only, ported from the PIEZO project (it carries no family constants): a recursive Newick parser that keeps IQ-TREE's `)aLRT/UFBoot:length` internal labels, bipartition extraction (`find_clade` is an **unrooted** test — a set and its complement are the same split, so paralog monophyly does not depend on where the arbitrary root lands), `pure_clades()` for the maximal same-group pieces of a broken clade, `reroot()` on the largest pure-outgroup edge, and `sister_of()` for reading a sister group off a rooted tree. The group vocabulary is read out of S6's `representatives.tsv`, so nothing here names a gene. |
| `s7_model.py` | **The substitution model, and why `-m MFP` did not choose it.** The brief's exhaustive scan was started, measured and abandoned: 11 of up to 1,232 protein models in 11 min 12 s, projecting **~20 h** — and the cost is the free-rate models, which are not optional here (`LG+R5` beats `LG+I+G4` by 682 BIC units on this alignment). The replacement is greedy in two stages with both tables committed: **A** every matrix at fixed `+G4`/`+I+G4`, **B** every rate model on the winning matrix. What that buys is stated rather than buried — the matrix winning under `+G4` need not win under `+R5` — and `model_selection.tsv` carries every model either stage scored, so the margin the greedy step was made on is checkable. |
| `s7_run.py` | **The S7 driver** — `prep` / `ml` / `bnni` / `au`. Threads are **pinned, not `-T AUTO`** (D24): AUTO reads the machine's current load and IQ-TREE is reproducible only at a fixed seed *and* a fixed thread count. The number pinned is the one IQ-TREE's own benchmark returned for this alignment (4; 8 threads is past the efficiency plateau), so pinning costs nothing. `bnni` re-runs the search with `--bnni`, UFBoot's own guard against the model violation a four-kingdom alignment guarantees — a check, not a second answer. `au` builds the three constrained ML trees and runs the AU test at 10,000 RELL replicates, writing `au_test.tsv` so the report renders from a table and not from IQ-TREE's own output (D13). Every stage resumes on the **`.iqtree` report, never the `.treefile`** — IQ-TREE writes a treefile at checkpoints, so a search killed part-way leaves a treefile of an unfinished topology that the next run would silently reuse — and `claim()` refuses a `--prefix` a live process already owns. Both guards are the same incident: a previous session's `au` was interrupted, its three constrained searches were orphaned and kept running for 50 minutes, a fresh `au` started three more on the same prefixes, and the AU test was handed whichever checkpoint treefile happened to be on disk. |
| `s7_constraints.py` | **The three sister hypotheses, and the membership they are asked about.** Each hypothesis is one split of the unrooted tree with the RyRs present to make it a rooted statement, and all three make every paralog clade monophyletic, so the AU test compares the sister arrangement and nothing else. **Membership is tree-corrected, and the correction is derived rather than listed**: a constraint built from census labels asks the test about a topology the data rejects for reasons unrelated to the sister question, and all three hypotheses then fail together (the PIEZO project hit exactly that and fixed it with a hand-written relabelling table; here the tree writes the table). **A constraint also names only the taxa its hypothesis is about.** IQ-TREE's `-g` places freely exactly the taxa the constraint omits, so a tip that is *listed* — even in a top-level polytomy — is pinned outside every group the constraint declares. The first version listed all 134 tips and thereby forced the unlabelled vertebrate tips out of the paralog clades this tree nests them in, identically in all three hypotheses; the AU test then rejected every one at ΔlogL ≈ 1,500, H3_23 included, which the ML tree itself holds at 100/100. `s7_test_tree.py` T5 now requires each constraint to name **no free tip**. Three rules — `core`, `reassigned`, `unconstrained` — and `home_of()` **stops at the tip's own nearest neighbourhood** rather than climbing until some paralog dominates: the first version climbed, and handed a mislabelled tip to whichever clade was largest three nodes up, which is a fact about clade sizes and not about the tip (`s7_test_tree.py` T6/T7 caught it). |
| `s7_analyse.py` | **S7 step 2** — the tree read once and turned into the committed tables everything else renders: `rooted.nwk`, group monophyly raw and tree-corrected, the membership audit, `claim_nodes.tsv` (SH-aLRT/UFBoot at every node a claim rests on) with `claim_members.tsv` beside it (the tip set behind each, so `s7_bnni.py` re-asks the *same* question rather than recovering a set from its size), the ML sister answer with its sister group's composition, the support summary, S6's cyclostome question asked of the tree, and the 3R duplicate-pair stress test. No report and no figure recomputes anything from the Newick (D13). |
| `s7_rbh.py` | **Reciprocal best hits on every naming call the tree contradicts** (the brief's requirement). Neither the alignment nor the model can tell a mis-annotated record from a tree error — both produced the placement in question — so the check is a different instrument entirely: the candidate against the human reference proteome, then that human protein back against the candidate's own species, pulled out of this project's vertebrate reference-proteome DB by the `OS=` field. A tip with no species proteome to run the reverse against is recorded `forward_only` rather than borrowing the forward hit's confidence. Targets are read from `naming_conflicts.tsv`, never listed, so the set verified is exactly the set the tree disputed — **every** row of it. Filtering to `reassigned`, as the first version did, made the step report "the tree contradicts no census label" while the table it had just read held five tips the tree declined to leave where their name puts them; a verification that cannot fire is not a verification. What `agrees` compares therefore depends on what the tree offered, and `compared_against` records it: a `reassigned` tip is tested against the tree's proposed paralog, an `unconstrained` one against the census name, because there the tree offers no alternative — only a refusal to place the tip. |
| `s7_test_tree.py` | **Negative controls for the tree utilities, run on every build.** Round-trip and support-label survival, `find_clade` as an unrooted test, `reroot` preserving every tip and leaving no unary node, `sister_of` — and the two that matter: **T5** parses each constraint newick back and requires it to hold the pair its name promises, with the third paralog outside it, the outgroup present (without which the split is unrooted and says nothing about sisters), each constrained tip named exactly once and **no free tip named at all** (a well-formed constraint can still encode a different, stricter hypothesis than the one it is named for, and the AU test would answer that one — which is exactly what happened), and **T6/T7**, the positive and negative halves of the reassignment rule — a weakly-supported placement must *not* overrule a census label, a strongly-supported one must. T6/T7 failed on the first run and the rule, not the test, was wrong. |
| `s7_figure.py` | S7's main figure — the rooted phylogram. Branches in neutral ink, the three paralog clades and the RyR outgroup boxed and coloured, a filled dot on every node clearing SH-aLRT ≥ 80 **and** UFBoot ≥ 95, a ring on every tip the tree relabelled. The relabelled set is **read from `membership_audit.tsv`, not listed in the figure**, so the rings and the constraint sets the AU test ran on cannot drift apart (D13 applied to a figure). The ring legend entry appears only when a ring does: on this tree the relabelling rule fires on nothing, and a key naming a marker the figure does not carry asserts a correction that was never made. Clade boxes are placed by iterating to a fixed point against the drawn label widths, because text width in data units depends on the x-limits and one measurement under the autoscaled limits underestimates it. |
| `s7_figures.py` | S7 figures 2–4, from committed S7 tables only (D13, D19). The AU panel does **not** put ΔlogL and p-AU on two y-scales — different measures, different scales, so two panels sharing one row of categories. The support figure is a scatter rather than two histograms because the claim is about the *joint* condition (both thresholds), which a pair of marginals cannot show. |
| `s7_bnni.py` | **Does any claim depend on the model rather than on the data?** UFBoot is optimistic under model violation and a four-kingdom alignment violates any single model by construction, so `--bnni` re-runs the search with an extra NNI round on every bootstrap tree. This module re-asks the *same* clade questions of that tree — membership read from `claim_members.tsv`, never re-derived, so a bookkeeping difference cannot surface as a topology change — and grades each `held` / `weakened` / `lost` / `strengthened`. It is a check, not a second answer: the reported tree stays the main search's, and a claim that lands in `weakened` or `lost` is one the paper must state with the `--bnni` number. `--tree/--label/--out` points the same comparison at **any** alternative tree, which is what answers the other question a set of searches raises — a *constrained* search can reach a better optimum than the free one (here the ITPR2+ITPR3 constraint did, by 4.8 logL units), and then the tree every table is built from is not the global optimum. A tree with no support labels, as a constrained search produces, is compared on topology alone and its verdicts read `present` / `absent`, because grading it `weakened` would report a loss of support that was never measured. |
| `s7_priors.py` | The priors S7's results are judged against, in one module imported by both halves of the report so a prior cannot be stated twice and drift. Each entry records what an earlier task concluded **and where it said it**; the report computes this tree's answer beside it and renders the verdict from the comparison. |
| `s7_report.py` + `s7_report_results.py` + `s7_report_naming.py` + `s7_report_checks.py` | **S7** — renders `results/phylogeny/report.md` purely from the committed tables (D13); scope, model selection, search and rooting in `s7_report.py`; the family-shape results (monophyly, the AU test, resolution, the `--bnni` check, caveats) in `s7_report_results.py`; and the tip-by-tip half — the names the tree disputes with their RBH verdicts, the cyclostome loci, the 3R stress test — in `s7_report_naming.py`, which also owns the support formatter every half reads a claim through; and the two checks on the tree itself — the `--bnni` guard and whether the reported tree is even the best one found — in `s7_report_checks.py` (the `s3_report.py` / `s3_report_d10.py` split, applied three times to stay inside the 500-line budget). Each check renders *not run yet* rather than nothing, so a check that was skipped is visible as skipped. **Headlines chosen by the data**: S6's identity preview picked a sister pair, S6's cyclostome table found all six loci nearest ITPR1, S6 named the teleost 3R pairs as a stress test, and every stage since S2 has assumed ITPR1/2/3 are clades. Each is stated in `PRIOR`, computed on this tree, and rendered `confirmed` / `contradicted` / `unresolved` with both numbers printed either way. |
| `s8_flank_lib.py` | **S8's data layer, and the three decisions that make a flank comparison mean anything.** Loci come from the per-genome `summary.json`, **not** the ledger: the ledger holds one row per genome × cell and therefore only the *best* locus, so a teleost ITPR1 cell's *itpr1a* would be compared against the next species' *itpr1b* and the mismatch reported as a synteny result. Two window rules, both committed, because naming density varies ~4× across this genome set (human 97 % of coding genes named, sea lamprey 27 %): `fixed10` is the brief's ±10 coding genes, `informative10` takes the 10 nearest *informative* symbols each side so a Jaccard difference cannot be an annotation-depth difference. And three key vocabularies — `strict`, `relaxed` (lowercase teleost duplicate suffixes stripped, uppercase symbols untouched) and **`root`**, the relaxed key with its trailing digit run removed, which is the only level at which a 2R ohnolog pair is visible because the two copies almost never carry the same symbol. |
| `s8_control.py` | **The null every S8 number is read against.** A Jaccard of 0.21 says nothing on its own, so every real pair *(locus in A, locus in B)* is scored against matched control pairs *(random coding gene in A, random coding gene in B)* under the same window and key rules — holding the two genomes, their annotation depth, their naming conventions and the normalisation constant. Sampling is seeded per accession (D24 applied to a random control) and refuses a contig too short to hold a window rather than degrading the null. The paired test is a **sign test**, ties dropped and counted, because Jaccard is bounded and zero-inflated and scoring both-zero pairs against the locus would turn an unannotated genome into a negative result. `background_prevalence()` is what tells a paralogon signal from a vocabulary artefact. |
| `s8_paralogon.py` | **S8's two family-specific questions.** The 2R paralogon — root families carried by both neighbourhoods in most species, each scored against its random-window background, counted at three prevalence bars so the answer does not rest on one line. And the **consensus paralog caller**: a per-paralog flank consensus with the query's own species dropped from every consensus first, calibrated on the loci whose paralog their own assembly's annotation establishes (evidence the caller never sees — it reads the neighbours' symbols, the truth is the gene's own). `frac_sweep()` measures call rate *and* random-window false-call rate at every threshold and `select_frac()` maximises their difference, because optimising call rate alone selects the loosest setting on offer, which is how an instrument gets tuned into agreeing with itself. `null_verdict()` puts the bar at the null's own maximum rather than a probability cut. |
| `s8_run_synteny.py` | **The S8 driver** — one pass per genome (gene table loaded once, both windows, control windows), then the matrices, every pair class against its matched null and stratified by vertebrate class, the consensus and paralogon tables, and the caller. `one_per_species()` is the pair-statistics subset rule. The RyR control cell is split by which bait won it, because it is one cell holding three genes and the pooled figure is a mixture. Runs `s8_test_flanks.self_test()` before writing anything and records the SHA-256 of every table. |
| `s8_tables.py` | The TSV writers. Every number the report renders comes from one of these files (D13); nothing downstream recomputes from the gene tables. |
| `s8_test_flanks.py` | **Negative controls for the synteny rules, run on every build** (`s5_bait_screen.self_test()`'s pattern). Eleven constructed cases, each rejected by the rule responsible: the placeholder vocabularies, the two symbol-normalisation rules in both directions, flank extraction excluding the locus and its overlappers, **empty-vs-empty Jaccard scoring 0 and not 1** (otherwise two unannotated genomes are a perfect synteny match), one-locus-per-species, control determinism, the caller's positive *and* negative halves plus its refusal to score a locus against a consensus it voted into, a threshold rule that refuses a setting calling 90 % of random windows, the null bar, and **T11 — ranked-table order invariance**, which caught a real hash-seeded nondeterminism in `flank_consensus.tsv`. |
| `s8_priors.py` | The priors S8's results are judged against, in one module imported by both halves of the report so a prior cannot be stated twice and drift. The verdict vocabulary is **five**-valued, and the three extra values are the ones that stop a comparison being overclaimed: `not corroborated` (a *related* property measured cleanly that does not support the prior — S7's sister pair against the retained flank ohnologs, which are duplication order and deletion history and not the same quantity), `orthogonal` (a different property of the same object — S5's recovery rate against neighbourhood conservation) and `underpowered` (the measurement did not reach the question, which is not a polite word for a negative: a Jaccard taken where 73 % of the flanking genes are unnamed is a measurement that did not happen). |
| `s8_figures.py` | The four S8 figures, from committed S8 tables only (D13, D19). The paralogon figure draws the three human neighbourhoods as gene tracks with the shared ohnolog families linked, reading the tracks out of `flanks.tsv` rather than naming any gene in the figure code. |
| `s8_report.py` + `s8_report_results.py` | **S8** — renders `results/synteny/report.md` purely from the committed tables (D13); scope and instrument here, results in the second half (the `s3_report.py` / `s3_report_d10.py` split). **Headlines chosen by the data**: S5's 812 `found_annotated` cells, S5's ITPR3 recovery, D14's family separation, S6/S7's teleost 3R pairs, S7's ITPR2+ITPR3 sister result and S7's explicit hand-off of the cyclostome question are each stated in `PRIOR`, computed on this instrument, and rendered `confirmed` / `contradicted` / `underpowered` with both numbers printed either way. |
| `s9_cds_lib.py` | **S9's data layer: a nucleotide sequence that provably encodes the protein S6 aligned.** dN/dS is a statement about codons, so nothing here trusts a translated database record. Three routes — Ensembl REST, UniProt cross-references (Ensembl → ENA → RefSeq `coded_by`), and the S5 genome gene models — and each is a **generator of candidates**, because a UniProt entry lists every transcript of its gene and the entry's own sequence is one particular isoform. Human ITPR1 lists five and ITPR2 two, and in **both** the first is not the isoform S6 aligned (**D36**): returning the first CDS that downloads silently substitutes a different isoform for the protein the tree was built on, which is a *wrong* codon alignment rather than a missing one. `validate_and_mask()` translates every candidate against the aligned protein and masks **every** disagreement to `NNN`, not only internal stops — a codon that encodes something else is not that protein's codon at that site whatever the cause, and codeml must see missing data rather than a residue that is not there. `tool_bin()` resolves the env-resident selection toolchain (D18). |
| `s9_miniprot_cds.py` | **Exact CDS for an S5 genome gene model.** The sweep ran `miniprot --trans`, whose `##STA` protein — the one in S6's alignment — *skips frameshifts*, so the GFF's CDS blocks run out of frame against it and splicing them is not offered here at all. The locus is realigned with `--aln`, whose `##ATA` row keeps the frameshift residues, and the reconstruction is accepted only if it reproduces the sweep's protein. `_best_alignment()` is the one concession, and it is measured: a locus rerun indexes 800 kb where the sweep indexed 3.2 Gbp, so a *terminal* exon can be placed differently (9 residues of 2,666 in *Hymenochirus* ITPR3). Same length and within 1 % is the same gene model and its disagreeing codons are masked; anything beyond that is a different model and is refused. |
| `s9_fetch_cds.py` | **S9 step 1** — a validated CDS for all 57 vertebrate family tips, concurrently, with every raw response cached and the miniprot route cached at the *result* level (a locus realignment is minutes, which no HTTP cache helps with). `cds_status.tsv` records the route, the source id, the candidates rejected on the way and why — "this tip has five transcripts and only one encodes the aligned isoform" is a fact about the record, not noise. |
| `s9_sets.py` | **Which tips are in which selection set, and why the tree decides.** codeml's branch models mark a *node*: a foreground that is not a clade does not fail, it marks a larger one and returns a well-formed ω for a hypothesis nobody asked. So the three sets are **S7's extended paralog clades**, re-derived here from `rooted.nwk` with S7's own rule and **cross-checked against `paralog_clades.tsv`** — a disagreement in size or membership is a hard failure. Two consequences: a tip S7 recorded as `unconstrained` is placed by the tree or by nothing, never by a database symbol; and the 7 unlabelled `vertebrate_basal` tips the tree nests *inside* a paralog clade join that set (D30 read forwards). The 6 tips in no clade stay in the whole-tree analyses as background — dropping them would change the branch lengths every other estimate is made on — and are in no foreground. |
| `s9_codon_aln.py` | **S9 step 2** — PAL2NAL codon alignment, cross-checked nucleotide-by-nucleotide against an independent in-house protein→codon mapping (a disagreement aborts), then trimAl `-automated1` columns chosen on the protein and applied **codon-aware, whole triplets only**. A residue whose codon step 1 masked is written `X` in the protein rows too, so pal2nal is never asked to reconcile a pair that disagrees — which it does by dropping the sequence, silently. Writes the per-paralog subsets, their pruned trees, and a **curated-CDS sensitivity subset** for every paralog that has genome gene models in it. Runs `s9_test_codon.self_test()` before writing anything. |
| `s9_codeml_lib.py` | Control-file construction, resumable execution and `mlc` parsing for PAML, plus the tree helpers the branch models need: `unroot()` and `labelled_newick()` (`$1` for a whole clade, `#1` for its stem alone). **`labelled_newick` refuses a non-monophyletic foreground** — the guard the entire branch half of S9 rests on. Also the χ² survival function and Benjamini–Hochberg, both stdlib. |
| `s9_jobs.py` | **The codeml job list, so a job cannot exist without a stated question**: per-paralog M0 (and its curated sensitivity twin), pairwise `runmode -2`, whole-tree M0, a two-ratio test per paralog clade, branch-site model A on each paralog stem, and M1a/M2a + M7/M8 within each paralog. **Branch-site model A is restarted from four initial ω by construction (D37)**, not repaired afterwards: a nested alternative cannot have a lower optimum than its own null, yet codeml reaches one routinely at this scale — the PIEZO project hit exactly that and had to add restarts after the fact. The spread across restarts is committed, so a stem the search is *still* stuck on is visible in the data. |
| `s9_codeml.py` | **S9 step 3** — the driver. Every job is resumable (an `mlc` that parses, or a `runmode -2` job's distance matrices, is reused), each runs in its own directory because codeml writes fixed filenames, and results are collected as they complete (`as_completed`, not `map`, so one slow site model cannot hold back the json a resumed run reads). |
| `s9_relax.py` | **S9 step 3b — HyPhy RELAX**, run locally so the version is pinned in the manifest rather than depending on a Datamonkey job id. Three runs: each paralog clade as the test set against the other two as reference. The tips the S7 tree places in no paralog clade are left **unlabelled** rather than swept into the reference — a branch whose paralog identity is unresolved is not evidence about either side of the contrast. |
| `s9_tables.py` | Every committed S9 table, written once from the codeml/HyPhy output so the report and figures never re-parse an `mlc` (D13). It also extracts **the fitted parameter every claim is actually about**, which the likelihood-ratio test does not report: `site_models.tsv` carries each site model's ω distribution and whether any class is *strictly* above 1 with a non-zero share, and `bs_restarts.tsv` carries each branch-site run's foreground ω₂, its share of sites and whether it sits at codeml's 999 bound. That is **D38** in code — three of S9's significant tests meant something other than what their p-values suggested, and in every case the tell was a parameter printed outside the test. Four further decisions live here: the branch-site LRT is tested against a **50:50 mixture** (its null fixes ω₂ = 1 on the boundary, so a plain χ²₁ p-value is twice too small — both are written out); an alternative that lands below its own null is flagged as the local optimum it is rather than reported; pairwise dS carries a **saturation flag**; and **BH runs across the whole LRT family**, because S9 asks the same test three times and the smallest of three uncorrected p-values is the error this project's self-tests exist to avoid. |
| `s9_priors.py` | The priors S9's results are judged against, in one module imported by both halves of the report. Reuses S8's five-valued verdict vocabulary unchanged, and `orthogonal` earns its keep here: two of S9's priors are about the *genome* (how well a paralog's neighbourhood travels, how often its locus is recovered) and ω is about the coding sequence, so calling a mismatch there "contradicted" would claim a disagreement between two things that were never measuring the same quantity. |
| `s9_report.py` + `s9_report_results.py` + `s9_report_models.py` | **S9** — renders `results/selection/report.md` purely from the committed tables (D13); scope and instrument here, the family-level results (purifying selection, saturation, the paralog contrast) in the second, and the model-by-model half (branch-site, site models, RELAX, caveats) in the third (the `s3_report.py` / `s3_report_d10.py` split applied twice). **Headlines chosen by the data**, and a verdict is **withheld** while a set is incomplete rather than taken on the paralogs that happened to finish first. Every significant branch-site or site test is printed beside its BEB posterior, because a significant LRT with no site above the 0.95 bar is the signature of a fit driven by saturation rather than by identifiable substitutions. |
| `s9_figures.py` | The four S9 figures, from committed S9 tables only (D13, D19). The ω panel is **logarithmic**: at ω ≈ 0.03 a linear axis puts every bar on the floor and hides the one number a reader wants, which is how far below 1 it sits. The saturation panel is **log–log**, because on a log-x/linear-y plot the neutral diagonal is not a line at all — the first draft's ω = 1 ran off the panel in the first pixel and left the *saturation* bar as the only line on the figure, which reads as neutrality. |
| `s9_run.py` | The S9b driver — ordered stages `codeml → relax → tables → report → figures`, `--only` / `--from` / `--list`, every stage resumable, non-zero exit if any stage failed. A failed stage does **not** stop the ones after it that do not depend on it: `tables`, `report` and `figures` render whatever has landed and the report marks an unfinished section *not run yet*, because waking up to a partial S9 that says which parts are partial is worth more than waking up to nothing. Ran the whole 22.7 h suite unattended. |
| `s9_test_codon.py` | **Twelve constructed negative controls, run on every build of the codon alignment** (`s5_bait_screen.self_test()`'s pattern): a frame-shifted CDS, a CDS encoding a different protein, an internal stop that must be masked and not refused, the property that an accepted CDS translates to its aligned protein modulo `X`, the in-house codon map's gap-exactness, whole-triplet gap stripping, **a non-monophyletic foreground being refused**, `$1`-vs-`#1` labelling, unrooting, the locus-rerun bound in all three directions, and the S7 cross-check being able to fail at all. These are checks on *refusal*: a codon alignment is the one artefact here where a silent error is invisible downstream, because codeml will fit a model to a frame-shifted alignment and return a perfectly plausible ω. |
| `s10_case_spec.py` | **Which annotation failures get validated, and why those two.** The measurement is `annotation_loss` — the share of a recovered gene's coding footprint that no single annotated model delivers, read off S5's own `frac_cds` (D13). Five eligibility rules, each a positive test, and **E5 is the one that does the work**: at least 10 annotated protein-coding genes elsewhere in the *same* genome must be longer than the locus. Without it the ranking's top rows are loci in assemblies with a genome-wide gene-length ceiling, and validating one would report a property of the whole gene set as a bug at this gene — measured over the sweep it removes 6 loci in 2 genomes (*Cirrhinus mrigala*, whose longest annotated gene anywhere is 42 kb; *Saguinus oedipus*, 138 kb against a 726 kb locus) and nothing else. Three failure modes are labelled (`omission` / `truncation` / `fragmentation`) and **two are selectable**, one case each, at most one per genome: taking the top two of the single ranking gives two omissions and leaves the fragmentation claim unvalidated. `control_strength` breaks ties — how many *other* family loci in the same genome the annotation gets right — the `s23_controls.control_strength()` idea that a control is graded, not merely checked. |
| `s10_gff.py` | S10's data layer: interval algebra, the assembly's `genomic.gff.gz` and the sweep's archived `miniprot.gff`, parsed separately and never merged (they say different things). **Pseudogenes are kept with their CDS blocks** — *Nibea albiflora*'s ITPR locus carries 51 — because dropping them would make S10 report "no coding model here" about a locus whose model the submitter demoted. CDS **phase is carried per block**, since a partial model's first coding block is often not the start of a codon. `read_miniprot_model` returns blocks in **target order**, not coordinate order, so a minus-strand gene's first block is the one carrying residue 1. |
| `s10_select.py` | **S10 step 1** — applies those rules to every locus the sweep recovered → `case_ranking.tsv` (the denominator: 880 loci, 382 eligible, 360 at loss 0), `annotation_depth.tsv` and `cases.tsv`. E3 calls `s5_calibration.spans_a_gene` rather than re-expressing D4's bar, so the ranking and the ledger cannot disagree. |
| `s10_evidence.py` | **S10 step 2** — every aligned exon classified by what the annotation holds *at that interval*, scored against annotated **CDS blocks and not gene spans** (a span covers its own introns, so scoring against spans credits the annotation with sequence it never called); every intron classified by which feature its two flanking exons fall in, which is what separates `within_one_model` from `between_models` — the junctions the annotation broke the gene at; disjoint coding blocks counted on both sides; and the **splice dinucleotides read off the genome**, strand-corrected. That last is a check on S10's own evidence: an alignment whose introns do not begin GT and end AG is an alignment, not a gene. |
| `s10_orf.py` | **S10 step 3 — is it really a pseudogene?** The annotation's `pseudo=true` is a falsifiable claim, so the locus is spliced with `s9_miniprot_cds` unchanged (twelve negative controls already wrapped around it) and its reading frame counted. The number it is compared against is **computed, not quoted**: the stops expected under neutral drift to the observed divergence, from the locus's own codon usage. Measured: all 8 family loci across the two case genomes carry **0 internal stops** against 4–25 expected. The verdict rule is deliberately one-sided — zero stops falsifies the pseudogene call; a handful would not establish it. |
| `s10_tile.py` | **S10 step 4** — annotated proteins **translated from the assembly's own GFF and genome**, not downloaded, because a pseudogene emits no protein record and the only way to ask what its model encodes is to translate it. Tiled by blastp against the genome's *own* recovered loci, so the winner names the locus the sequence belongs to. `at_own_locus()` is the guard the naming verdict rests on: the subject set is the census-v4 non-redundant models, a query whose own locus is missing from it lands on its nearest paralog instead, and the row then reads as a confident naming disagreement — which is exactly what happened before the check existed. |
| `s10_probes.py` | **S10 step 6 — is the exon structure a gene, or an alignment?** Two attacks on S10's own evidence. ±90 nt CDS probes across every junction the annotation does not model, searched against the nucleotide databases restricted to the species, counting only hits **contiguous across the junction** (≥ 8 nt each side) — batched into one remote search per case, and **reported with its denominator**, which for these two species is the result. And, offline and stronger here, **exon-boundary concordance**: every swept genome whose same paralog was aligned from the same bait and whose annotation independently agrees with the alignment is a genome where miniprot's boundaries have been corroborated by another pipeline; 94.5 % and 100 % of the two cases' boundaries are shared by a majority of 35 and 40 such genomes. Stated for what it is — it validates the instrument at this gene, not the individual locus. |
| `s10_context.py` | **S10 step 5** — the assembly-version audit (the assembly report, the GFF3 header's `genome-build-accession`, and the live-then-cached `datasets` list of every assembly for the species), plus the two inventories that turn a coordinate finding into a claim about databases: every family-named model in the whole annotation with the biotype that decides whether it emits a protein, and every protein record the census holds for the species. Also `flank_consensus_check()`, which reads **S8's committed consensus flank set** and asks whether an unannotated locus's neighbours are the ones its paralog usually has — a fifth, independent line: *Nibea albiflora*'s missing ITPR2 sits between SSPN and BHLHE41, this paralog's two most conserved neighbours across 197 and 183 of 215 swept vertebrates. |
| `s10_tables.py` | The committed tables and `annotation_bugs_stats.json` — rules, parameters, self-test status and the SHA-256 of every table (S6/S8/S9's discipline). |
| `s10_figures.py` | The four S10 figures, from committed tables only (D13, D19). The exon track draws exons at **true genomic width and never widens them to be visible**: a 56-exon gene over 52 kb averages 143 bp an exon, and fattening them would draw a gene whose coding fraction looks like 40 % when it is 15 % — which is the one thing the figure exists to show. |
| `s10_report.py` + `s10_report_cases.py` | **S10** — renders `results/annotation_bugs/report.md` purely from the committed tables (D13); scope, selection and what the task changes about earlier tasks here, the two case studies in the second half (the `s3_report.py` / `s3_report_d10.py` split). Each case is written in the order a reader needs it — what the annotation says, then what the DNA says, then the five checks — because a case study that opens with its own validation asks to be believed before it has said anything. |
| `s10_run.py` | The S10 driver — ordered stages `select → evidence → orf → tile → probes → figures → report`, `--only` / `--from` / `--list`, `--no-remote` to skip the network half and keep the offline one. Runs `s10_test_evidence.py` **before writing anything** and refuses to write if it fails. A failed stage does not stop the ones after it that do not depend on it, and the report marks an unfinished section *not run yet*. |
| `s10_test_evidence.py` | **Forty-seven constructed negative controls, run on every build** (`s5_bait_screen.self_test()`'s pattern). Checks on *refusal*, because every rule in S10 returns a plausible number when it is wrong: interval algebra on the cases that break naive code; **a canonical minus-strand intron reading GT–AG rather than CT–AC** (getting this wrong makes every minus-strand gene look non-canonical, a systematic signal that would discredit exactly the alignments this task defends); an exon in a passenger gene's intron not counting as annotated coding; the four intron classes; a junction hit crossing by 1 nt not counting; a minus-strand model splicing in target order; each eligibility rule firing on its own violation; selection being order-invariant and one-per-genome; a tiling row whose best hit is not at its own coordinates yielding **no** naming verdict; the boundary control being able to report a boundary nobody shares; and a GFF3 pseudogene keeping its CDS blocks, its phase and its name. |
| `s11_lib.py` | **S11's acquisition and caching layer.** AlphaFold DB, RCSB file download, UniProt records, and one on-disk cache under `<data_root>/raw_api/s11/` where a 404 is stored as the literal `null` so a re-run does not re-ask. `afdb_probe()` is the one that earns its keep: AFDB is keyed on a UniProt accession but answers with **whatever record it holds**, which may be an isoform — asked for the three human paralogs it returns `Q14643-4` (2,695 aa of 2,758), `Q14571-2` (**181 aa of 2,701**) and the canonical `Q14573`. It therefore ranks every record in the response, prefers the queried accession, records what was actually served, and exposes `is_canonical` and `coverage()` so "AFDB has a model for this accession" is never conflated with "this protein is modelled". |
| `s11_struct_io.py` | A deliberately small mmCIF/PDB CA reader — Biopython is not used, because S11 needs only CA coordinates per chain, the residue numbering and the B-factor column (where AFDB puts pLDDT), and what a full parser adds is failure modes on exactly these files. `largest_chain()` is **the tetramer trap**: both families are homotetramers and an IP3R assembly is ~11,000 residues, so every reference is reduced to one chain before it is scored or TM-align would answer a question about quaternary structure. `write_ca_pdb()` sidesteps the 99,999-atom PDB ceiling these assemblies blow straight through. |
| `s11_tmalign.py` | TM-align execution and parsing. Both normalisations are kept — a full-length model against a partially resolved cryo-EM structure scores very differently by each, and which one is meaningful depends on the question. `TM_RANDOM` (0.17) and `TM_FOLD` (0.50) are TM-align's own published bars, not thresholds this project chose, and the report draws them rather than describing them. The length/score regexes match both `Chain_N` and `Structure_N` wordings, because a version-string change silently yields zeros for every pair, which reads as "nothing folds like anything". |
| `s11_rcsb.py` | **The RCSB query layer** — brief step 1's "resolve the references by query, not from memory". The candidate set is enumerated from the family's own Pfam signatures (`src/utils/family.py`), *not* filtered to cryo-EM at enumeration, so the report can show that this family's X-ray entries are all truncations rather than assert it. Enumerating on the **union** of the four signatures is load-bearing: RCSB's Pfam annotation of human ITPR3 (6DQN) carries PF02815/PF08454/PF01365/PF00520 and **not** PF08709, the signature that names the family — the same absence S2 measured on 2,911 of 15,417 proteins — so a PF08709-only query would have missed the project's own IP3R reference. Entry and entity metadata come through one batched GraphQL call instead of ~600 REST calls. |
| `s11_refs.py` | **Which structures are references and which are controls — seven rules, each a positive test.** **R1** the family call comes from *this project's census* on the entity's UniProt accession, never from an entry title (the IP3R and RyR entries share every diagnostic Pfam and half their titles). **R2/R3** cryo-EM with a recorded resolution, and full length in the family's declared band — a RyR cannot enter through the ITPR band because the band was defined to exclude it. **R4** the conformational state must be readable from the title against a stated vocabulary, ordered so compound states are tested before the words they contain (`inactive-like` before `active`). **R5** one reference per paralog, and a paralog the census does not name gets **no** label rather than falling back to the family name — a group called `RYR` beside RYR1 and RYR2 would assert a paralog nothing named. **R6** the state panel, on the paralog offering the most states, because a model scored against one conformation confounds fold with gating. **R7** negative controls come from **S1's committed decoy panel**, one per class, held to the same cryo-EM and full-length rules and size-matched to the family reference; a class with no qualifying structure is reported unfilled. |
| `s11_afdb_probe.py` | **S11 step 2 — how much of the family AlphaFold DB actually holds,** measured three ways that do not agree: UniProt's `in_alphafold` cross-reference, the API probe, and *model coverage* against the length the census holds. Only the third can see the isoform trap. Accessions that are not UniProt-shaped (genome gene models from S5/S23, Ensembl ids) are `not_applicable` and stay out of the coverage denominator rather than counting as misses. Groups are normalised through `s6_lib.normalise_group`, because the census carries `Viridiplantae` and `viridiplantae` both. |
| `s11_panel.py` | **S11 step 3 — the panel and its manifest.** **P1** every selected reference and control, reduced to one chain. **P2** every S6 representative AFDB models, at whatever coverage — coverage is a *column*, never a filter, because the reason a representative has no usable model is the result. **P3** group fill: where a taxonomic slot has no representative model, the best-covered census record stands in and `fill_rule` records that the slot was *filled* rather than met; without it the panel is six proteins. One vocabulary for slots, and it is the census's — S6's representative table calls a group `protist` where the census has four, so a slot audit built from both at face value reports `plant` unfilled beside `Viridiplantae` filled. **P4** nothing enters unread: every file is parsed, its largest chain measured, and `model_coverage` is *resolved residues over the census length*, not what the API advertised. `predicted` decides whether the B-factor mean is written to `mean_plddt` or to `mean_bfactor` — AFDB writes pLDDT there and a cryo-EM deposition writes an ADP, which on this panel runs to 122. |
| `s11_tmalign_run.py` | All-vs-all TM-align, resumable, cached on the **SHA-256 of the two files** so a changed structure re-runs rather than silently reusing a stale score. The extra pairs beyond vs-reference are what calibrate the scale: same-protein-different-state bounds how much of a TM difference is conformation, cross-family is D14 asked structurally, and anything against a control is the floor. `self_test()` runs on every build — a self-pair must score 1.0 (the cheapest check that the manifest's paths point where they say), every pair must have parsed a score, and the two normalisations must bracket each other. |
| `s11_plddt.py` | **Per-domain confidence, boundaries transferred by pairwise alignment.** A mean pLDDT over a 2,700-residue multi-domain channel averages a well-predicted β-trefoil with hundreds of residues of linker and looks reassuring while saying nothing about the part a claim rests on. Domains come from S0's measured coordinates on the human paralogs; which paralog to transfer from is decided by measurement, and the metric is **covered-only identity** and not `pairwise_align`'s raw BLOSUM62 score — the first version ranked the three references on the score and printed "identity = 10181.00" into a table. A domain landing on under `MIN_DOMAIN_COVER` of its reference span is reported `placed = 0` with no pLDDT, and every model gets an *outside annotated domains* contrast row, without which "the pore is at 85" has nothing to be high against. |
| `s11_tables.py` | Every committed S11 table, plus `structures_stats.json` (parameters, self-test status, SHA-256 per table). The one table that is not a dump of a previous step is `tm_vs_reference.tsv` — **D14 asked structurally**: best TM against the IP3R references, best against the RyR references, and a **relative** margin between them (`s3_assign.py`'s reason — an absolute gap would call every full-length structure and no fragment). Scores are taken **normalised by the reference**, which is the whole difference between a fold statement and a size statement: a 1,100-residue fragment normalised by its own length only has to explain itself. |
| `s11_foldseek.py` | The optional AFDB sweep, scoped and *named*: the Swiss-Prot subset, written into every output row, so a negative is a negative about a declared space rather than an unbounded "nothing exists". Hits are resolved to UniProt taxonomy and scored against the census — already-ITPR is a confirmation, RYR is D14 arriving by a third instrument, and unseen is the lead. |
| `s11_priors.py` | The priors S11's results are judged against, in one module imported by both halves of the report. Reuses S8's five-valued verdict vocabulary unchanged; `orthogonal` matters here more than anywhere, because almost everything before S11 measured *sequence* and S11 measures *shape* — two proteins 25 % identical that share a fold do not contradict the 25 %. One prior is the brief's own advance guess that AFDB coverage would be poor, written down before it was measured. |
| `s11_report.py` + `s11_report_results.py` | **S11** — renders `results/structures/report.md` purely from the committed tables (D13); scope, the AlphaFold coverage measurement and the instrument here, the fold results in the second half (the `s3_report.py` / `s3_report_d10.py` split). **Headlines chosen by the data**: every prior is stated in `s11_priors.PRIOR` with where the earlier task said it, computed from S11's own tables, and rendered with both numbers printed either way. The comparison that has to be sayable is the one that goes badly — if a census record did not fold like an IP3 receptor, the table it would be said from is `tm_vs_reference.tsv`. A section whose table is absent renders *not run yet*. |
| `s11_figures.py` | The four S11 figures, from committed S11 tables only (D13, D19). The calibration panel **draws** TM-align's two published bars rather than printing them in a caption — a calibration figure that asked to be believed would not be one. The coverage figure's second panel is coverage against *record length*, which is where the result lives: the usable-model mass sits below ~1,300 residues while the peak at ~2,700 — a full-length subunit — is almost entirely unmodelled. |
| `s11_test_structures.py` | **Negative controls for the S11 rules, run on every build** (`s5_bait_screen.self_test()`'s pattern). Checks on *refusal*, because every rule here returns a plausible number when it is wrong: a second mmCIF model must be ignored and altloc B skipped; the CA trace must round-trip residue ids and the B-factor column (losing it loses every pLDDT); `largest_chain` must be deterministic on a tie; compound states must beat the words they contain; **R1 must call a RyR a RyR when the title says IP3 receptor**; a binding-core construct and a RyR-length chain must both fail the ITPR band; a fragment control must lose to a size-matched one; and a structure scoring equally against both families must come back `no_call` rather than the higher one. Mutation-tested on three deliberate rule breakages, all three caught. |
| `s11_run.py` | The S11 driver — ordered stages `afdb → panel → tmalign → plddt → foldseek → tables → figures → report`, `--only` / `--from` / `--list`, `--with-foldseek` (off by default: it is the one stage needing a multi-gigabyte index). Runs `s11_test_structures.py` **before writing anything**. A failed stage does not stop the ones after it that do not depend on it, and the report marks an unfinished section *not run yet*. |
| `s12_panel.py` | **S12's scope, derived from S10's ranking rather than chosen.** Four rules, each a positive test: **P1** every locus S10's five eligibility rules admit whose annotation loss clears `LOSS_FLOOR` — read straight off `case_ranking.tsv`, because re-deriving eligibility here would let two tasks disagree about which loci the annotation could reasonably have delivered; **P2** the species has at least `MIN_RUNS` public Illumina RNA-seq runs, a floor on *askability* and not on power; **P3/P4** every other ITPR locus the sweep recovered in the same genome joins the reference as an internal control, and so does the RyR locus (D14's sister family, whose reads must land on RyR). A control locus carries the loss S10 measured for it or **none at all** — `loss` is `None`, not `0.0`, because a locus S10 never ranked and a locus measured at zero loss must not share a cell. |
| `s12_lib.py` | S12 shared plumbing: paths, the permanent on-disk HTTP cache, SRA E-utilities (`SraRun`, search and XML parse), the tissue vocabulary with **attribute-verified** assignment (a run found by a "brain" query whose own attributes say "liver" is rejected, and which attribute the call came from is recorded), FASTA/TSV IO and the dashboard live panel. `normalise_tissue` takes the **longest** keyword match, so "head kidney" cannot depend on dict order; `tool_bin` searches PATH and **both** conda envs (D18) — S12 straddles them, `sra-tools`/`hisat2` in `s12` and blast in `piezo1`, and a one-env lookup reports a present tool as unavailable. |
| `s12_locus.py` | **The module every S12 number rests on** — a recovered locus turned into a reference sequence with tagged junctions. The reference is the **spliced genomic** CDS (miniprot's blocks from the archived sweep GFF, revcomp'd on the minus strand, concatenated in target order), deliberately *not* S9's frameshift-corrected CDS: correcting a frameshift inserts 1–2 bp that every read crossing it would carry as an indel, costing reads at exactly the difficult sites. Validation is **colinear block placement**, which has no tuned threshold — each block is translated in its own phase and searched verbatim in the sweep's `##STA` protein *from the previous block's match onwards*, and a block may fail to place only if the model's own frameshift count explains it. The identity test it replaced was the wrong instrument and measuring it showed why: a correct reference scores 1.00 with no frameshifts and 0.92 with nine. Junction classes come from S10's `classify_introns` unchanged and are matched **by genomic coordinate, never by index** — a coordinate-sorted and a target-sorted list have the same length and opposite order on a minus-strand gene. |
| `s12_refs.py` | **S12 step 1** — the per-species reference: every panel locus, three housekeeping anchors, and a reversed decoy per sequence (identical length and composition, no homology, so what it collects is this reference's spurious-mapping floor). The anchors are aligned with **miniprot and spliced by the same module as the targets** rather than pulled from each GFF: two of the three annotations name no genes at all (*D. mawsoni* files all 29,240 as "hypothetical protein"), and building anchor and target alike means a difference between them cannot be a difference in construction. Bait accessions are **resolved by query against a declared length band**, never remembered — the first version hard-coded three and got all three wrong, including a 427 aa "GAPDH" and an accession that served nothing. `--trans` on the miniprot call is load-bearing: without it there is no `##STA` line, every model has an empty protein and the coverage filter rejects them all, which reads as "this genome has no housekeeping genes". |
| `s12_runs.py` | **S12 step 2** — the run selection, keyword-driven but attribute-verified, with every considered run and its verdict committed. Tissue-directed queries first (they spread the selection over the organs a species has libraries for), then a **capped** tissue-blind pass whose cap is spread over distinct studies before depth: *Nibea albiflora* alone returns 114 qualifying runs, and streaming them all would re-ask one question of one lab's libraries where taking the deepest run of each study buys independence. Per-species run counts are written out, because P2 excludes a species for want of libraries and that exclusion has to rest on a number in a table. |
| `s12_quantify.py` | **S12 step 3** — `fastq-dump -X … \| hisat2` per run, SAM parsed in flight, only counts to disk. `--no-spliced-alignment` is deliberate: the reference is already CDS, so a junction read is *contiguous* here and leaving spliced mode on would let hisat2 open a gap inside the CDS and call an intron that does not exist. Counts reads, junction-spanning reads and covered bases per sequence **and every junction on its own** — "this gene is transcribed" and "these particular junctions, which the annotation does not have, are spliced" are different claims and only the second answers S10. Junctions with no reads are written as zeros, never omitted, because the denominator is half the result. |
| `s12_tables.py` | Every committed S12 table, written once from the per-run counts so the report and figures never re-parse a counts file (D13). Carries the three-part detection rule with the clause that failed named on every row, and two things the PIEZO port did not have: `junction_support.tsv`, which scores each junction separately by whether the annotation models it, and `annotation_gap_coverage.tsv`, the read-level form of S10's annotation-loss measurement and the one S12 number directly comparable with it. Records the self-test status and every tool's version in `expression_stats.json` alongside the SHA-256 of each table. |
| `s12_atlas.py` | **S12 step 5 — the independent cross-check.** The same junction-probe test S10 ran and could not answer, re-asked on a panel that now includes *Dissostichus mawsoni* and its **37,166 mRNA records** against the other two species' 43 and 10. Two controls, both necessary: the annotated junctions of the same genes probed identically, and S10's genomic negative control unchanged (180 nt of contiguous *spliced* sequence cannot span its junction, so many hits and zero spans is the expected answer). Reuses `s10_probes` throughout; the one change to S10 is a defaulted `classes` argument on `build_probes` so the annotated junctions can be probed too. |
| `s12_crossmap.py` | **S12's closed-set control, measured rather than argued away.** The reference is a closed set, so if the paralogs were similar enough at nucleotide level for a read to cross between them the task would be measuring the wrong gene — and the ITPR paralogs are more similar to each other than the PIEZO family this method was ported from, whose S12 dismissed the risk in a caveat. Every reference is tiled exhaustively with 100 nt synthetic reads at 10 nt steps and mapped back under the same aligner settings; the result (0 of 12,500 reads assigned elsewhere) is committed whether it is zero or not. Tiled, not sampled, so it is exhaustive over positions and deterministic. |
| `s12_figures.py` | The four S12 figures, from committed S12 tables only (D13, D19). The junction figure is **per junction, not per gene** — a bar saying "this gene has junction reads" is the claim S10 could nearly already make. The decoy floor is **drawn, not stated**. Counts are on log axes because these libraries differ ~40× in depth and the comparison that matters is within a run. The "annotation loses this gene" mark is a *marker*, not a text glyph: `figstyle.save()` refuses a figure needing a glyph the document font lacks, and it refused this panel's first version over a multiplication sign. |
| `s12_priors.py` | The priors S12's results are judged against, in one module imported by both halves of the report. Reuses S8's five-valued vocabulary; `underpowered` does real work here, because S10 wrote in prose that a search of 43 records returning nothing shows only that the species has no deposits, and S12 has to be able to reach that verdict **about its own** deposit cross-check. `orthogonal` matters because S12 measures transcription where everything before it measured sequence or annotation: a silent locus would not contradict an intact reading frame. |
| `s12_report.py` + `s12_report_results.py` + `s12_report_checks.py` | **S12** — renders `results/expression/report.md` purely from the committed tables (D13); scope, the reference construction and the instrument here, the results in the second, and the part of the report that argues against itself — the deposit cross-check, whose verdict on its own power is `underpowered`, and the caveats — in the third (the `s3_report.py` / `s3_report_d10.py` split, applied twice). Every prior is stated with where the earlier task said it, computed on S12's own tables, and rendered with both numbers printed either way. §5 reports the annotated-vs-unannotated recovery ratio **with its confound printed beside it**: several libraries are strongly 3′-biased and a truncated gene's annotated junctions are the 5′ ones, so the ratio would report a property of library preparation as a property of the annotation — the claim this task makes is the absolute one. |
| `s12_test_expression.py` | **Eleven constructed negative controls, run before anything is written** (`s5_bait_screen.self_test()`'s pattern). Target-order splicing, a block outside the fetched window refusing rather than shortening the reference, colinear placement rejecting reversed order and the wrong strand, phase-driven translation, junction classes matched by coordinate on a **two-intron minus-strand** gene (the smallest case that can tell a coordinate lookup from an index lookup — a one-intron test cannot, and a mutation test found exactly that mapping slipping through), an unmatched junction kept rather than dropped, CIGAR arithmetic, and the anchor rule in both directions. Mutation-tested on five deliberate rule breakages, all five caught. |
| `s12_run.py` | The S12 driver — ordered stages `refs → runs → quantify → atlas → tables → figures → report`, `--only` / `--from` / `--list`. Runs the self-test **before writing anything**; a failed stage does not stop the ones after it that do not depend on it, and the report marks an unfinished section *not run yet*. |
| `s13_species_tree.py` | **The accepted species tree as an input, not a result (D15).** A hand-curated topology for the 31 vertebrate species S6 samples, 29 named internal nodes, every one carrying a literature age, the spread of published estimates and its source; `--check` fails on a gene-tree species missing from the tree, a taxon with no gene-tree tip, an uncalibrated node, a point age outside its own spread or any age inversion. Two things it does that a cladogram-with-ages does not: where the literature does not resolve an arrangement the node is left a **polytomy** rather than given a shape the sources do not carry, and every node carries a **`stem_age_ma`** beside its crown age — the sampled tree omits every unsequenced lineage, so a node's parent *here* is usually older than the split that created it, and a duplication on Boreoeutheria would otherwise be bracketed [96, 319] Ma instead of [96, 99]. The deep calibrations are identical to the PIEZO project's because both read the same sources: a shared **input**, not a ported result. |
| `s13_lib.py` | S13's reconciliation core, stdlib: `SpeciesIndex` (depth/parent/LCA), `reconcile()` (Goodman/Page LCA mapping; losses per Zmasek & Eddy 2001), `extract_clade` (which **refuses** a clade carrying labels outside the requested set), `prune_tips`, `collapse_unsupported`, `reroot_at`, and the paralog sets read out of S7's committed `paralog_clades.tsv` rather than re-derived. **The duplication test is the non-binary one (Vernot et al. 2008)** — a duplication when a child maps to M(v) itself *or* when two children land in the same child-subtree of M(v). The familiar binary rule is only valid on a binary tree, and on the support-collapsed root (a four-way polytomy mapping Cyclostomata / Gnathostomata / Cyclostomata / Gnathostomata) it reads a speciation, which would have reported the collapse as the duplications disappearing. `collapse_unsupported` **refuses a tree with no support labels**, because a constrained IQ-TREE search writes none and the first run dissolved all three constrained topologies into one 134-tip polytomy. Species keys go through `s6_lib.binomial`. |
| `s13_reconcile.py` | **The matrix** — 5 topologies (the ML tree, the three AU-scored sister constraints, the `--bnni` re-search) × 3 variants (all tips / the six cyclostome loci actually pruned / every node below S7's own SH-aLRT 80 + UFBoot 95 bar collapsed) = 12 reconciliations, 3 refused with their reason. Constrained trees are re-rooted on the RyR outgroup before the vertebrate subtree is taken, because reconciliation on an unrooted tree is meaningless. `bracket()` uses the committed **stem age**, not the sampled parent's, and reports the root's bracket as open rather than closing it silently. Also `rooting_check()` — every one of the 112 edges of the vertebrate subtree re-rooted and scored, with the outgroup rooting in the same table as `edge_index -1`, so the two criteria are read off one scale. |
| `s13_losses.py` | **The loss audit, and the rule it could not do without.** Every species × paralog cell is asked of the S5 genome ledger: in the tree / in the genome but unsampled / `paralog_unassignable` / corroborated loss / loss with a remnant / undecidable / out of scope. **`paralog_unassignable`** is the positive test that stops a bait-panel limit reading as biology — run without it the only four corroborated losses were ITPR2 and ITPR3 in the two cyclostomes, whose genomes carry **three ITPR loci apiece**, all filed in the ITPR1 cell because S5 has no cyclostome-labelled bait. It fires on two independently committed numbers, the genome's spare locus count (S5) and the species' tree-unplaced tips (S7), and writes both into the row. A `fragment` is undecidable, not an absence (D25). |
| `s13_cyclostome.py` | **The six tips the deep placement rests on.** Two questions, because they decide how much weight the answer carries. (1) Does an instrument that never saw the alignment agree with the tree's three hagfish/lamprey orthology pairs? The join to S8's per-locus flank calls is exact and offline — the *Myxine* tips are S5 gene models and carry their own coordinates, the *Petromyzon* tips are UniProt records whose census-v6 `gene` field is the same `LOC` symbol S8 recorded. (2) Are they long branches? Cyclostomes are the classic long-branch-attraction risk and long branches are attracted to the root, which is exactly where the answer sits — so root-to-tip distance is measured for all 57 vertebrate tips rather than the objection argued either way. |
| `s13_priors.py` | The priors S13's results are judged against, in one module imported by both report halves so a prior cannot be stated twice and drift. Reuses S8's five-valued verdict vocabulary; `orthogonal` earns its keep because S8's retained flanking ohnologs are a **deletion** record while a duplication placement is a **branching** statement, so a mismatch between them is not a disagreement. |
| `s13_test_recon.py` | **Fourteen groups of constructed negative controls, run before anything is written.** Checks on *refusal*, because every rule in a reconciliation returns a plausible number when it is wrong — and two of them did: **T2/T3** are the halves of the non-binary duplication rule, **T6** is the collapse refusing an unlabelled tree. Also `extract_clade` rejecting a superset, pruning leaving no unary node, rerooting at every edge preserving every tip, one species under two spellings mapping to one tip, all ten loss verdicts firing on their own case, four hand-derived Zmasek & Eddy loss counts, the species-tree validator being able to *fail*, determinism, and the paralog sets matching S7's committed table. Mutation-tested on three deliberate rule breakages, all three caught. |
| `s13_figures.py` | The four S13 figures, from committed S13 tables only (D13, D19). The dated backbone is drawn on a **linear time axis with the calibration spread as a band**, not a cladogram with ages written on it: the result is an interval and a cladogram cannot show one. Placements are deduped to one marker per distinct (variant, node, paralog set), because the matrix repeats the same placement across cells and drawing each would make agreement look like weight. |
| `s13_report.py` + `s13_report_results.py` + `s13_report_audit.py` | **S13** — renders `results/reconciliation/report.md` purely from the committed tables (D13); scope, the species tree, the instrument and the negative controls in the first, the placement and what it depends on in the second, and the half that argues against the headline — the cyclostome cross-check, the loss audit, what the result does and does not settle, the caveats — in the third (the `s3_report.py` / `s3_report_d10.py` split, applied twice as S7 did). Both halves read the matrix through `s13_lib.matrix_cells` / `deepest_nodes` so they cannot disagree about how many reconciliations ran. |
| `s13_run.py` | The S13 driver — ordered stages `species_tree → reconcile → losses → cyclostome → stats → figures → report`, `--only` / `--from` / `--list`. Runs `s13_test_recon.py` **before writing anything** and refuses to continue if it fails; a failed stage does not stop the ones after it that do not depend on it, and the report marks an unfinished section *not run yet*. |
| `s15_lib.py` | S15's data layer and the three things worth naming in it. `load_summaries()` reads the sweep's archived per-genome `summary.json` and **not** the ledger, because the ledger holds one row per genome × cell and therefore only each cell's *best* locus, while the spare loci are what tell `paralog_unassignable` from an absence (D45). `known_loci()` reproduces `s5_run_sweep.filter_hsps_outside`'s exclusion set — every locus the aligner clustered anywhere in the genome, any bait, padded — so every reconstruction in S15 is computed on HSPs *outside* it and a reference cannot be reassembled out of the genome's other paralogs' genes; the cross-paralog control is inherited from the sweep rather than re-invented. And `spearman()` / `sign_test()` / `youden()` are stdlib, because S15's confounder controls are a rank correlation and a paired test and adding a third-party version number inside a negative result is not worth the convenience — the t-tail is validated against published critical values in `s15_test_loss.py`. `_fmt` writes floats at `%.6g` and not four decimal places, after the first build wrote a p-value of 2.1e-07 as `0.0000` and the report read it back as zero. |
| `s15_reconstruct.py` | **How much of a reference protein an assembly holds when it holds none of it as one gene.** The sweep's `tblastn_trace` cells are the ones a loss count can neither ignore nor use, and what is measured here is the non-redundant coverage of the reference *reassembled across contigs*. Four properties make it a positive test: it is computed outside every locus the aligner found; it is **one reference at a time**, never a union over baits (a union over three orthologous baits reports a coverage no single protein achieves); `unique_frac` tells a tiling from a pile-up, which is what separates a shattered gene from fragments of three mutually 61–68 % identical paralogs stacking on the conserved blocks; and `exonic_nt` / `gene_equiv` count on the **subject** side, because reference coverage cannot count copies but genomic positions can. `region_index()` splits the regions into **three** scopes and not two — `own_clade`, `decoy_accounted` and `co_trace` — which is D46. |
| `s15_calibrate_recon.py` | **The bar a reassembly has to clear, measured rather than chosen.** The negative costs no new search: regions the full 38-bait panel attributes to a paralog whose gene the aligner already placed at a locus in the same genome, so the fragment set cannot be that gene. Three refusals. It does not quote a threshold without its separation (`separation()` reports Youden's J beside the bar, `s23_calibrate_loci.separation()`'s rule — and the first pass here *did not* separate, at J = 0.52). It does not put the operating point at Youden's own threshold, which on a perfectly separated pair lands on the lowest positive and is the most permissive bar the data allow — `operating_point()` takes the gap's midpoint and commits both edges, so a later run whose populations have drifted into contact is visible in the table. And `bar()` **raises** rather than handing out a threshold the calibration marked unusable, with `MIN_POS`/`MIN_NEG` floors so it cannot pass vacuously (`s20_test_sensitivity.py`'s rule applied to a calibration). |
| `s15_synteny_reach.py` | **The brief's first step, and the number that answers it.** S15 was asked to disambiguate the sweep's undecided cells by synteny with measured accuracy; S8's caller already exists and is calibrated, so the work is to point it at the trace regions and measure whether it *arrives*. It does not: of 432 rescue regions, 273 sit on a contig carrying no annotated gene at all and 8 reach the caller's four-key floor. `accuracy_by_keys()` puts S8's own leave-one-genome-out calibration on the same axis, so the accuracy quoted is the accuracy available at the key counts the traces actually have (100 % at every count the caller acts on) — which is what makes *accurate and unavailable* a statement rather than an excuse. The window rules and symbol vocabularies are S8's, imported unchanged, so the two tasks cannot disagree about what a flank is. |
| `s15_integrity.py` | **Is the reading frame broken, or the assembly?** Lesions are counted per kilo-aligned-residue, never per gene, or the 1.8×-longer RyR reference leads every ranking by construction. `covariates()` measures density against the three things that could produce it without a gene being dead, and the one that matters is not the one the brief names: contig N50 barely moves it (ρ = −0.077) and bait identity moves it a great deal (ρ = −0.397), because a poorly matched reference buys alignment with frameshifts (**D47**). So `paired_within_genome()` — the control that removes the assembly entirely, each ITPR cell against the *same genome's* other family loci — is run twice, once identity-matched, and the difference decides the claim. Ties dropped and counted (S8's rule); the four tests are one family and are BH-corrected together (S9's rule). The bar is the upper quantile of a population the screen never scores: full-coverage loci above D4's bar whose own annotation names them family. The verdict is deliberately one-sided — `s10_orf.py` established that zero stops falsifies a pseudogene call and a handful does not establish one. |
| `s15_species_tree.py` | **The tree the loss count is placed on, declared as an input (D15) and built where the losses are.** S13's curated 31-species tree cannot serve: 278 of the species whose cells the matrix holds are not in it. So the topology is NCBI taxonomy over all 309 assemblies, from the same archived `datasets` dumps S4 built the manifest from plus one archived call for the 991 ancestor names — fully offline thereafter. One tip per **accession**, not per species, because the sweep's unit of observation is an assembly. `_suppress_unary` keeps the *deepest* named node of a unary chain, so a node's label is the most exclusive taxon containing exactly its tips. `polytomy_report()` commits the degree distribution, because a count of independent losses under a polytomy is bounded by the resolution. And `compare_with_s13()` checks compatibility rather than assuming it, asked so the two trees' different *sampling* cannot register as a disagreement — the first version counted assemblies of species S13 never sampled as intruders and reported 21 of 29 clades as unrecovered while every matched node carried the right name. |
| `s15_states.py` | **The character matrix's vocabulary and the ordered rules that assign it.** The S5 ledger's statuses were not built to license a loss count — `absent` there means the rescue attributed no region, which in a shattered assembly is a statement about contig lengths — so S15 re-states every cell under one constraint: **exactly one state may be counted as a loss**, reached only by passing every positive test that could explain the absence otherwise. Eight states, first-fires-wins, each writing the number it fired on into its row: `no_control` (R0, the sweep's own control rule), the four `present_*` states (R1–R4), `paralog_unassignable` (R5, D45 — the cyclostomes), `undecidable_contiguity` (R6, D4) and `absent` (R7). `spare_loci()` recomputes S13's definition from the per-genome summary rather than the ledger and reproduces its committed counts exactly. |
| `s15_matrix.py` | The matrix itself, both evidence axes side by side and never merged, plus `implied_copies()` — the per-genome statistic that makes it countable. Reference coverage cannot count copies (a genome holding only ITPR1 recovers most of the ITPR2 reference at 65 % identity, which is why the calibration needed a decoy at all); genomic sequence can, because a placed locus and a reassembly occupy different places, so the per-cell contributions add. Spare family loci are added too, or the cyclostomes read as carrying one gene when the sweep placed three. `loss_candidates.tsv` commits the near-misses beside the losses, each naming the rule that stopped it, because that list *is* the sensitivity of the count. |
| `s15_tables.py` | Every committed S15a table, written once, plus `loss_dynamics_stats.json` (rules, parameters, self-test status, SHA-256 per table) and `headline()` — the numbers the report is not allowed to compute for itself (D13). |
| `s15_priors.py` | The priors S15 is judged against, in one module imported by both report halves so a prior cannot be stated twice and drift. Reuses S8's five-valued vocabulary; two values do real work. `underpowered` is the honest verdict on the brief's own synteny step — a comparison run where 273 of 432 regions have no neighbourhood is a measurement that did not happen, and calling it a negative would report contig lengths as biology. `orthogonal` is what stops the ORF screen reading as a disagreement with S9: lesion density counts indels in an assembly and ω is a ratio of substitution rates. |
| `s15_figures.py` | The four S15a figures, from committed S15 tables only (D13, D19). The matrix figure orders 309 genomes by contiguity and draws D4's bar, so a reader can see that the red the S5 ledger showed is gone *and where it went*; the state colours come from `figstyle.STATUS` so a cell red here is red for the same reason it was red in S5's figures. The calibration figure **draws** the gap's two edges rather than printing the bar in a caption. The synteny figure is the joint distribution of what a trace region has to work with, not a bar of call rates, because the claim is about availability. Density axes are logarithmic **with a zero band**, since 72 % of intact loci carry no lesion and a linear axis puts the whole calibration population on one pixel. |
| `s15_test_loss.py` | **Fifteen constructed negative controls, run before anything is written** (`s5_bait_screen.self_test()`'s pattern). Checks on *refusal*, because every rule here returns a plausible number when it is wrong: a reconstruction without the known-locus filter reassembles a missing gene out of its own paralogs and reports 0.95 (T2); a union over baits reports a coverage no protein achieves (T3); the same genomic interval counted twice inflates the copy statistic (T4); a three-deep pile-up must score `unique_frac` 0 and a clean tiling 1 (T5); a cell beside spare family loci must be `paralog_unassignable` and not `absent` (T6); a genome whose control failed must support nothing (T7); **the loss state must be reachable** — T8 constructs a contiguous, controlled, empty, spare-free cell and requires `absent`, because a state nothing can reach is not a negative result; the decoy must exclude co-shattered paralogs (T9); the calibration must refuse to pass vacuously (T10) and Youden must report non-separation (T11). Mutation-tested on three deliberate rule breakages, all three caught. |
| `s15_run.py` | The S15a driver — ordered stages `recon → synteny → integrity → tree → matrix → tables → figures → report`, `--only` / `--from` / `--list`. Runs `s15_test_loss.py` **before writing anything** and refuses to continue if it fails; a failed stage does not stop the ones after it that do not depend on it, and the report marks an unfinished section *not run yet*. |
| `s15_report.py` + `s15_report_results.py` | **S15a** — renders `results/loss_dynamics/report.md` purely from the committed tables (D13); scope, the state vocabulary, the instrument, the synteny measurement and the negative controls in the first, the results in the second (the `s3_report.py` / `s3_report_d10.py` split). **Headlines chosen by the data**: every prior is stated in `s15_priors.PRIOR` with where the earlier task said it, computed from S15a's own tables, and rendered with both numbers printed either way — and the comparison that has to be sayable is the one that goes badly, which here would be a row of `character_matrix.tsv` in state `absent`. §11 is written as a hand-off: what the half settles, the four things it does not, and the two things a reader should hold against it. `pfmt()` exists because a p-value rendered at four decimal places is `0.0000`, which hides how strong a claim is rather than how weak. |
| `s20_groups.py` | **S20's scope, and why it is that scope.** The six sweep groups — the four non-vertebrate eukaryote ones that partition Eukaryota with S3's `vertebrata` (so the two sweeps' denominators add), plus archaea complete and a genus-stratified bacterial sample. `largest_per_genus` is the sampling rule, and it is deliberately generous to the hypothesis it tests: one proteome per genus, the one with the *most* proteins, because a larger proteome is a more sensitive place to find a homolog and picking it makes the negative harder to obtain. Also `EVALUE_PRIMARY` / `EVALUE_RELAXED`, the two sensitivities every claim is made at. |
| `s20_lib.py` | S20 shared plumbing: paths, group manifests, the live panel — and **`accession_to_upid()`**, the exact hit → proteome attribution. The sweep runs against one concatenated DB, which loses which proteome a sequence came from, and the obvious substitute (matching on taxid) is wrong: 3 protist, 18 plant and 27 fungal taxids carry **two** reference proteomes each, so a taxid key credits both with either one's hits and inflates the presence numerator. The attribution is measured by a header-only pass over each proteome file and cached under the data root. |
| `s20_fetch.py` | **S20 step 1 (data)** — the six groups' reference proteomes, reusing S3's download and concatenation unchanged so the two sweep DBs are built identically. Two additions: the bacterial genus stratification, and **a 404 is an exclusion, not an abort** — UniProt's proteome list and the release FTP tree are not always in step, a listed-but-unpublished proteome 404s permanently, and retrying cannot fix it; it is recorded in `proteome_unavailable_<group>.tsv` with what it took out of the denominator. Every other failure still aborts, because those a re-run does fix. |
| `s20_taxa.py` | **S20 step 2** — taxid → ranked lineage for every swept proteome (`--groups`) and every census v4 record (`--census`, so v5 carries lineage columns on every row, D8), batched through UniProt's taxonomy API and archived under the data root so the parse redoes offline. The coarse `group` bucket is **imported from `s2_uniprot.GROUPS`, not re-declared**: v5 stacks on S2's counts, and a second bucket table would make "Viridiplantae 0/15 in S2" and "Viridiplantae N in S20" counts of different things. A taxid the API does not return gets an empty row rather than no row. |
| `s20_profiles.py` | **S20 step 5's profiles** — the four family Pfam HMMs (PF08709, PF02815, PF01365, PF08454) downloaded from InterPro and committed, so the relaxed negative claim rests on more than one full-length model. Three of the four are shared with something else, which is the point: for a *negative* claim a promiscuous profile is the right instrument, and anything it finds is then handed to the D14 call rather than counted. |
| `s20_run_sweep.py` | **The S20 sweep driver** — both S3 profiles over each group's DB, then S3's margin assignment, plus the relaxed panel over the negative-claim groups. **One search, two sensitivities**: every `hmmsearch` runs at `-E 10` and the primary E ≤ 1e-5 call is taken by filtering the same domtblout, because `-E` is a reporting threshold and does not touch the acceleration filters — so the strict set is exactly what a strict run would have produced and the relaxed set is a superset from the same search, not a second experiment. Writes the per-proteome presence table, which is the range result's raw form. |
| `s20_evidence.py` | **S20 step 3's evidence layer** — the four independent lines a plant/fungal verdict can rest on, gathered for every candidate whether or not they agree: sequence (from the archived FASTAs, so the chase reruns offline), the record's own UniProt paperwork (fragment, existence level, EMBL and proteome cross-references), the nearest neighbour *outside* its kingdom by blastp, and the nearest *within* it. The third is the load-bearing one: a genuine deep homolog is 20–40 % identical to its metazoan relatives, an assembly contaminant is 95–100 % identical to one particular animal. |
| `s20_verdicts.py` | **S20 step 3** — an individual verdict on every plant and fungal ITPR record, from *both* instruments pooled (census v3 and this task's sweep; taking only the sweep's would drop the UniProtKB entries that are not in a reference proteome, which are most of the interesting ones). Seven rules in order, each a positive test that writes the number it fired on into the row: `unresolved` / `module_only` (under D22's 200-state floor) / `contaminant_suspect` (≥ 95 % across kingdoms) / `cross_kingdom_outlier` (80–95 %, flagged not called) / `no_genome_backing` / `fragment` / `real_gene`. |
| `s20_jackhmmer.py` | **S20 step 4** — jackhmmer to convergence from a seed native to each group, against that group's own DB. **The seed is derived, not chosen**: the highest-scoring ITPR call in the group's own sweep whose match spans at least half the model, written to `jackhmmer_seeds_s20.tsv` before anything runs so a rerun on a changed sweep shows up as a changed seed. D10 is evaluated by `s3_kill.evaluate` on the per-round included lists parsed out of the archived log, exactly as in S3. |
| `s20_census_v5.py` | **S20 step 6** — census v4 + this sweep → census v5, under **S3's merge rule unchanged (D23)**, because a new rule here would make v5's calls incomparable with v4's. Every row gains lineage columns from one source (D8), with v4's own columns kept beside them rather than overwritten, and every plant/fungal row carries its verdict — "census v5 holds N Viridiplantae records" means nothing without saying how many survived being chased. |
| `s20_figures.py` | The four S20 figures, from committed S20 tables only (D13, D19): the range as a fraction of *swept proteomes* rather than of records (so a well-sequenced phylum cannot outvote a sparse one), D14's separation outside the vertebrates with the no-call band drawn, the plant/fungal verdicts beside the cross-kingdom identities the contamination test rests on, and per-group jackhmmer convergence beside the sister-family trace K1 is evaluated on. |
| `s20_test_sensitivity.py` | **The equivalence test for S20's one-search-two-sensitivities design**, and the module that proved the design's stated assumption wrong. S20 runs every search at `-E 10` and filters at E ≤ 1e-5 on the belief that `-E` is a sequence reporting threshold and the two files must therefore match below the cut. They do not: `--domE` is applied to the **conditional** E-value, which is normalised by how many sequences passed, so a looser `-E` inflates every c-Evalue by a constant factor (1.41× measured) and pushes marginal domains out of the report. Measured over 798 protist targets: the sequence sets and **all 798 assignments** agree and no D22 gate decision moves, but 11 domain rows differ and one evidence class with them — every differing row scores at or below 0 bits, and a −2.0-bit alignment spanning 1,386 match states counts as coverage once merged. The task uses the relaxed side, which excludes them. A second effect appears only at scale: HMMER prints the sequence E-value to two significant figures, so a target whose true E-value sits just above 1e-5 prints as `1e-05` and a `<=` filter admits it while a strict run never reports it — 42 of 13,770 plant targets on the RyR profile, all at 31.2 bits and **all declined by the D22 gate**. The pass criterion is therefore about the *call*, not set equality: no assignment moves, no D22 gate decision moves, and no one-sided target is called. Also refuses to **pass vacuously**: its first run went green on `archaea`, which has no ITPR hit at either threshold, so a comparison of fewer than 25 targets is now a failure with its own message. |
| `s20_report.py` | **S20** — renders `results/s20_sweep/report.md` purely from the committed tables (D13); scope and instrument here, results in `s20_report_results.py`. |
| `s20_report_results.py` | The results half, split to keep both under 500 lines and taking the caller's loader and formatter (the `s3_report.py` / `s3_report_d10.py` pattern). **The headlines are chosen by the data**: S2's plant and fungal findings were measured on a *seeded* space a protein enters only by already carrying a family Pfam, so "Dikarya has no records" could as easily have meant "no records with a family Pfam annotation". Each section states S2's number in `PRIOR`, computes S20's, and renders `confirmed` / `contradicted` / `underpowered` from the comparison, printing both either way. |
| `s23_scope.py` | **S23's genome scope, and why it is that scope.** Five rules, derived from S20's committed presence table rather than typed, under one principle: **sample most finely where the negative claim is.** `phylum_rep` (one assembly per non-vertebrate eukaryotic phylum), `class_rep` (one per class in the phyla S20 swept ≥ 100 proteomes of), `absence_clade` (one per class in every clade swept ≥ 10 proteomes with **zero** ITPR — which is how it re-derives S20a's target list *and* finds three absences that summary never named: Bacillariophyta 0/16, Rhodophyta 0/12 and **Cestoda 0/11, a metazoan clade**), `anchor` (the one hand-written list, deliberately: an anchor is a genome whose answer is known from the literature and no table derives that), and `copy_number` (the highest-copy species per class above the vertebrate paralog count — *Macrostomum* 62, *Paramecium* 39, *Cymbomonas* 36). `control_clades()` enumerates the clades a positive-control bait must come from. |
| `s23_bait_spec.py` | **The seven rules that choose the S23 panel**, and the three that are not S5's. **B1** a bait's label is its clade band, never a paralog — ITPR1/2/3 are a 2R product and the question outside the vertebrates is copy number. **B2 S5's RyR positive control does not transfer**: S20 found architecture-level RyR in 2 of 6,928 non-metazoan proteomes, so a land plant with no RyR locus is the *correct* answer and RyR cannot be its proof-of-search — the universal control is the **MIR-domain sharer**, in the family's own signature set, drawn from the clade each claim is about, and simultaneously the sharpest decoy. **B3/B5** the length band and the architecture-exception floor are **measured**, per band (`stratify`), and `passes_shape()` raises until they have been: measured over the whole population they are the arthropod family's numbers (53 % of it) and they excluded *Bodo saltans*, the deep euglenozoan record the panel exists to carry. `control_strength()` grades each control strong or weak rather than presenting both as "controlled". |
| `s23_controls.py` | **The positive control, one level down from S20's.** PF02815 over each group's archived reference-proteome DB, hits attributed by the `OS=`/`OX=` fields HMMER preserves in its own description column (exact in this direction, unlike proteome attribution), then one bait per control clade. `clade_coverage()` measures what fraction of a clade's swept proteomes carry any MIR protein, which is what makes the Apicomplexa control honest: **1 PF02815 protein per apicomplexan class across 60 proteomes**, so that absence claim rests on a 233 aa control found once. Its first build used a 600 aa floor chosen to select mannosyltransferases and silently left five clades — both apicomplexan classes among them — with no control at all. |
| `s23_span_calibration.py` | **The two genomic thresholds, measured for this scope.** Gene spans from NCBI's *annotations* for genes the census independently calls ITPR — deliberately not from miniprot, whose own `-G` shapes the loci it reports, so a measurement taken that way cannot falsify the setting it calibrates. A span bounds the largest intron, which is the direction `-G` needs. **Per group, because the family's span varies ~100× outside the vertebrates against 6.5× inside**: metazoan genes median 83 kb, protist and fungal 7–9 kb. The panel is derived from census v5 by gene symbol *or* protein name — symbols alone reach 8 bands, names reach 28. |
| `s23_calibration.py` | Those thresholds read back rather than typed (D13 applied to constants). `max_intron_for()` is floored at miniprot's own default, because a `-G` below an unmeasured species' largest intron splits its gene; `contiguity_bar()` returns *how* the bar was derived alongside it, so a group with no measurement of its own (Viridiplantae) is visibly borrowing the global median. |
| `s23_manifest_lib.py` | Selection and writing for the manifest, importing S4's parsing and ranking primitives rather than copying them. What is new is the **size guard**: `rank_key_s23` puts contiguity *first*, because an annotated assembly that cannot carry the gene is worse evidence for an absence than an unannotated one that can, and a clade whose best assembly is still below the bar is kept and flagged rather than dropped. `by_ancestor` indexing is load-bearing: NCBI files a reference assembly under the *strain* taxid, so four of fourteen anchors — the apicomplexan, the microsporidian, the chytrid and the amoebozoan — missed a species-taxid lookup. |
| `s23_build_manifest.py` | **The S23 denominator** → `results/s23_scope/genome_manifest_s23.tsv` + `manifest_stats.json` + `absence_coverage.tsv`. 194 genomes, 100 Gbp, from 24,596 NCBI eukaryote reference assemblies less 6,216 vertebrate ones. Every `datasets` call cached under the data root. |
| `s23_bait_select.py` | Measuring, ranking and filling the S23 bait slots — split out to keep both halves under 500 lines (the `s3_report.py` / `s3_report_d10.py` pattern). `measure()` stratifies the length band and the architecture floor **per band**; `fill()` applies B3's spread rule to the *shortlist* rather than to its output, which is the distinction that cost the panel its *Chlamydomonas* bait on the first two builds — a score-ranked shortlist of six never reached it past seven higher-scoring *Cymbomonas* records, and filtering afterwards removed a bait without replacing it. |
| `s23_build_baits.py` | **The S23 panel builder** — census v5 → 68 baits (ITPR + RyR + MIR control) by B1–B7, screened with **S5's `s5_bait_screen` module unchanged** (now taking a spec module, so one screen serves both panels and S5's own negative controls still run on every build). `unfilled_slots.tsv` names the stage that lost each slot — no record / fails shape / no sequence / fails the screen — because its first version blamed the census for seven bands, four of which have dozens of records. |
| `s23_classify.py` | **Copy number, not paralog cells.** Loci won by the ITPR baits (D14) are graded `full` / `fragment` / `scrap`, and the genome's copy number is its count of `full` ones. `merge_split_loci()` folds neighbouring same-strand loci whose bait spans are *complementary*, recording both counts, because a split gene inflates the very number this task reports — the error S5 could tolerate (its best locus always won) is the one this task cannot. `control_verdict()` decides whether the search demonstrably reached the assembly: the MIR control is universal, the RyR control additional where a RyR is expected, and a genome failing both supports no absence claim. |
| `s23_run_sweep.py` | **The S23 driver** — per genome: fetch, annotation, miniprot at the group's measured `-G`, copy-number classification, and a family-level tblastn rescue where the ITPR baits win nothing. Resumable per genome, `--reasons` selects by scope rule, `--delete-after` for fetch → search → purge. |
| `s23_ledger.py` | Assembles every completed `summary.json` into the deliverable tables (D13 — the summaries carry the calls). `absence_at_genome.tsv` is the one the negative claims are read off, and it counts **only controlled genomes** in both columns. |
| `s23_control_profiles.py` | **The candidate control profiles, and why each is a candidate.** S23a replaced S5's RyR control with the MIR-domain sharer (D26) — right for land plants and Dikarya, wrong for Apicomplexa, where both classes carry *one* PF02815 protein across 60 swept proteomes and the pilot's *Toxoplasma* came back `uncontrolled`. The fault is not PF02815; it is **fixing one profile in advance for every clade**. Six candidates (MIR, Myosin_head, E1-E2_ATPase, SMC_N, Kinesin, AAA), every one a large deeply-conserved multi-exon eukaryotic family, so whichever a clade selects the control exercises spliced alignment over a long gene rather than one small domain. Downloads them from InterPro beside S20's four. |
| `s23_control_select.py` | **Which profile controls which clade, decided by measurement.** Runs each candidate over each clade's own swept reference proteomes and takes the one that is actually there — highest clade coverage, ties to the longer protein, with the whole table written out so the choice reads as a measurement and a clade where *every* candidate is thin is visible as such. `AAA` is deliberately last and used as the fallback it is. `control_strength()` grades on clade coverage, bait length and how much of the profile's own **model** the bait carries — read off the domtblout's model coordinates, because a 900 aa protein matching 15 % of the model is a fragment of the control family however long it is. |
| `s23_calibrate_loci.py` | **What counts as a locus, and what counts as one copy** — the two S5 thresholds S23a inherited without measuring, measured from the sweep's own output. The identity floor is measured against loci whose identity the assembly's **own annotation** establishes, which is evidence the alignment score did not produce; the sweep records every cluster down to `RECORD_MIN_IDENTITY` precisely so the floor is not measured from the population it has already filtered. Reports how many `no_locus` genomes sit within 0.05 of the floor (the brief asked), and locus span against CDS footprint per group. **Refuses to write below 50 genomes / 15 confirmed loci**: a smoke-test run over one genome produced 0.25 from two loci, wrote it, and the next sweep read it back and moved three genomes from `no_locus` to `fragment_only` — the `s20_test_sensitivity.py` rule against passing vacuously, applied to a calibration. **What it found (S23c) is a negative that changed the instrument**: the annotation axis is nearly empty outside the vertebrates (21 of 917 clusters carry an informative gene name), so a second axis was added — every recorded cluster scored against `itpr.hmm`/`ryr.hmm`, re-derived from the archived GFFs — and **neither identity nor coverage separates** the confirmed and contradicted populations (Youden J 0.70 / 0.71). `separation()` reports both statistics and `profile_gate_audit()` scores the replacement gate against the one axis it does not share (10/10 and 10/11). |
| `s23_copy_number.py` | **What counts as one gene**, split out of `s23_classify` to keep both under 500 lines. Two rules pulling opposite ways: `merge_split_loci` folds two neighbouring same-strand clusters whose bait spans are *complementary* (one gene the aligner broke), and `full_copies` splits one cluster into the distinct complete models inside it, because a cluster is not a gene (**D28**) and with `-G` at 650 kb a cluster is a large object. Measured effect over the 194-genome sweep: **7 genes recovered in 7 genomes** (174 copies against 167 clusters). |
| `s23_locus_evidence.py` | **What independent evidence says about each locus** — split out of `s23_calibrate_loci` to keep both under 500 lines, and the basis on which any threshold on alignment score can be calibrated at all. Two axes, and they are not equally good: the assembly's **own annotation** knows nothing about this project's baits, profiles or family definition, and outside the vertebrates it is nearly empty (21 of 917 clusters carry an informative gene name); the **profiles** (`itpr.hmm`/`ryr.hmm`, D23) are a different algorithm on different data but not independent of the family definition, and their loci are re-derived from each genome's *archived* miniprot GFF so the calibration re-checks offline. `is_placeholder()` is a positive test on the name — treating `LOC594527` as a contradiction would have put two correctly-recovered genes in the junk population. |
| `s23_figures.py` | The four S23 figures, from committed S23 tables only (D13, D19): copy number by group with the vertebrate paralog count as a reference line rather than a category, the absence claims with the **controlled**-genome bar drawn beside the searched one (a claim whose control bar is short is standing on nothing), the measured identity floor with the two populations it separates, and locus span ÷ CDS footprint. |
| `s23_census_v6.py` | **Census v5 + the non-vertebrate sweep's gene models → census v6**, under the same merge rule as v4 and v5 (D23): every model scored against `itpr.hmm`/`ryr.hmm`, models the profiles decline reported and not enrolled, and `db_status` recording *how* a database holds the locus. Two things differ from v4: there are no paralog cells, so a row carries a **copy index** and its genome's copy number; and `annotated_unnamed` is the large class rather than the exception, because outside the vertebrates most gene models carry locus tags — *Chlamydomonas* files its receptor as `CHLRE_16g665450v5`. |
| `s23_report.py` | **S23** — renders `results/s23_scope/report.md` purely from the committed tables (D13). Titles itself from the scale it is rendering (`s5_report.py`'s rule: a 14-genome pilot and a 194-genome sweep must not be able to wear each other's heading); scope and instrument here, results in `s23_report_results.py`. |
| `s23_report_results.py` | The results half, split to keep both under 500 lines and taking the caller's loader and formatter (the `s3_report.py` / `s3_report_d10.py` pattern). **Headlines chosen by the data**: S20a's absences were measured on *proteomes* — what a gene-caller found — and S23a's pilot answered 14 genomes, so each section computes its statistic, compares it against the recorded prior in `PRIOR`, and renders `confirmed` / `contradicted` / `underpowered`, printing both numbers either way. The comparison that has to be sayable is the one that goes badly: a receptor found in an assembly whose proteome had none means the absence was a database fact. |
| `s5_genome_io.py` | Genome-file plumbing, ported unchanged from the PIEZO project (it carries no family constants): faidx-style offset index, region fetch, N-run detection, NCBI sequence/assembly report parsing, GFF3 download via `datasets` slimmed to a per-gene table, and `GeneIndex` for overlap queries. |
| `fetch_genomes.py` | **The genome downloader** → `<data_root>/genomes/<acc>/` — md5-verified against the `md5sum.txt` NCBI packs in the zip, resumable via `.done` markers, retrying download+extract+verify *together* (a truncated zip surfaces as an md5 failure, not a download error). Installs only what verifies, then rewrites `md5sum.txt` to the files actually kept so `--verify` treats a missing name as a failure instead of skipping it. `--search-cmd '… {fna} …' --delete-after-search` gives fetch → search → purge operation at one genome of peak footprint; a failing search keeps its genome. `--only`, `--limit`, `--max-gb`, `--smallest-first`, `--dry-run`, `--purge` (guarded to stay inside the genomes root). |
| `build_findings_page.py` + `findings_page.css` | Renders `docs/findings_summary.md` to one self-contained HTML page, inlining every linked figure as a downscaled WebP data URI, with a paralog summary card read live from the committed tables (and omitted entirely until they exist, so it cannot show a card of zeroes). |

Each ledger task adds its own `s<n>_*.py` here. The PIEZO project's
equivalents (`../piezo_genes/scripts/`, mapped in its `INTERFACE.md`) are
reference implementations worth reading first.

## `src/utils/family.py` — the family definition

**The single place the project's subject is defined.** Pfam signatures
(`FAMILY_PFAM_IDS`), the census signatures, the size band
(`MIN_LENGTH_AA`/`MAX_LENGTH_AA`, set to exclude the ~5,000 aa ryanodine
receptors), the reference panel (human ITPR1/2/3), the sister-family panel
(RYR1/2/3), the literature keyword, and the known-name substrings. Pointing
this app at another family is an edit of this file plus the presets.

## `src/core/` — data models, search orchestration, caching

| File | Key types / functions |
|------|-----------------------|
| `models.py` | `ProteinVariant`, `GeneRecord`, `SearchQuery`, `SearchResult`, `SearchStatus`. |
| `cache.py` | `DiskCache(root, ttl_s)` — JSON-file cache keyed by `(source, query)`. |
| `search.py` | `SearchOrchestrator(cache, email, …)` — fans queries across enabled DBs on worker threads, marshals results back via `queue.Queue`. Special-cases Foldseek (waits for sequence clients, harvests bait sequences). Re-exported **lazily** from `src/core/__init__.py` (PEP 562) so importing `src.utils` does not drag in biopython — the session-protocol data-root check has to run in a bare interpreter. |

## `src/databases/` — one client per source, common interface

| File | Class | Notes |
|------|-------|-------|
| `base.py` | `DatabaseClient` | Abstract; subclasses implement `search()`. |
| `ncbi.py` | `NCBIClient` | Biopython `Bio.Entrez` against the `protein` index. |
| `ensembl.py` | `EnsemblClient` | `rest.ensembl.org` `/lookup/symbol/{species}/{symbol}` → `/lookup/id?expand=1` → transcripts → translations. **S0 changed the symbol-resolution path**: `/xrefs/symbol/homo_sapiens/…` stalls indefinitely (per-species server fault, `BRCA2` too), while `lookup/symbol` works and `xrefs` still works for other species — so `xrefs` is now only a fallback. Evidence: `results/s0_baseline/ensembl_endpoint_probe.tsv`. |
| `uniprot.py` | `UniProtClient` | `rest.uniprot.org/uniprotkb/search`; surfaces canonical + named isoforms. |
| `alphafold.py` | `AlphaFoldClient` | `alphafold.ebi.ac.uk/api/prediction/{acc}` — pivots off UniProt; reports pLDDT and model URLs. |
| `foldseek.py` | `FoldseekClient` | `search.foldseek.com/api/` — async structure-based remote-homology search; opt-in. |
| `compara.py` | `ComparaClient` | **Phylogenomic paralog mining** via Ensembl Compara gene trees — every within-species paralog of the query genes, regardless of naming. Flags adjacent family loci as possible split gene models. Note: ITPRs and RYRs share a gene tree, so its paralog lists include RyRs by design. |
| `blast.py` | `BlastClient` | **Sequence-bait BLAST** via NCBI's public queue — blastp of the longest fetched sequence per gene against nr; finds homologs however they are named. Async, 2–10 min. |
| `interpro.py` | (functions) | InterPro/Pfam helpers — `list_proteins_with_pfam()` (census mode: `max_results=None` paginates to exhaustion, with `strict` failure, raw-page `dump_dir`, `stats` out-dict, and — added in S2 — `start_url`/`start_page`/`on_page` for a resumable walk, `CursorExpired` for a rejected cursor, exponential backoff to a 60 s cap, and **two-host failover** across `/interpro/api/` and `/interpro/wwwapi/`, which fail independently: during S2 the documented host answered 1 request in 12 while the website's host answered 12 of 12), `batch_fetch_family_signatures()`, `has_family_signature()`, `fetch_uniprot_sequence()`. Not in `AVAILABLE_CLIENTS`; used by discovery + domain scan. |

`AVAILABLE_CLIENTS: dict[str, type[DatabaseClient]]` in
`databases/__init__.py` is what the orchestrator looks up by name;
`SEQUENCE_SOURCES` / `STRUCTURE_SOURCES` group them for the UI, and
`BAIT_SOURCES` (`BLAST`, `Foldseek`) marks clients the orchestrator runs
*after* the sequence DBs, feeding them the longest fetched sequence per gene
as bait.

## `src/gui/` — tkinter/ttk widgets

| File | Class | Responsibility |
|------|-------|----------------|
| `app.py` | `MainWindow` | Top-level window, menus, status bar, queue polling, export, bundle save (+ report). Exposes `run(project_root, email)` and `load_variants()`. |
| `search_panel.py` | `SearchPanel` | Gene + species + DB checkboxes + preset loader; routes `"mode": "domain_scan"` presets to `on_special_preset`. |
| `results_view.py` | `ResultsView` | Sortable / filterable `ttk.Treeview` of variants; `add_context_action()` adds right-click items. |
| `details_view.py` | `DetailsView` | Read-only labels + clickable URL + scrollable sequence preview. |
| `tools.py` | `ToolsController` | The **Analysis menu**: sequence analysis, novel-paralog discovery, domain-bait scan, deep-dive investigation, selection test (dN/dS), fold check (ESMFold), presence/absence matrix, and `augment_bundle()`. |
| `dialogs.py` | `AnalysisDialog`, `DiscoveryDialog`, `InvestigateDialog`, `DomainScanDialog`, `SelectionTestDialog`, `FoldCheckDialog` | Modal option dialogs; read `.result` after construction. |
| `task_runner.py` | `TaskRunner` | One-at-a-time worker-thread bridge, results marshalled back via queue + `after()`. |
| `text_window.py` | `TextWindow` | Reusable scrollable monospace output window with Save-as. |
| `phylo_view.py` | `PhyloWindow` | **Alignment & tree viewer**: canvas phylogram (novel tips ★-marked) over a conservation-shaded alignment overview. |

## `src/analysis/` — alignment, tree, clustering, mutations, evolution

| File | Key types / functions |
|------|-----------------------|
| `alignment.py` | `pairwise_align()`, `progressive_msa()` (star / MAFFT-optional). |
| `distance.py` | `identity_matrix(covered_only=False)` → `DistanceTable`; `covered_only=True` scores identity over mutually covered columns only (fragment-aware). |
| `tree.py` | `build_nj_tree()`, `tree_to_newick()`, `tree_to_ascii()`. |
| `clusters.py` | `cluster_variants()` → `ClusterReport` with novel-candidate flags. |
| `mutations.py` | `call_mutations()` → list of `MutationCall` vs a reference. |
| `evolution.py` | `conservation_per_column()`, `variable_regions()`. |
| `selection.py` | **dN/dS** (Nei–Gojobori 1986, pure Python): `ng86()`, `fetch_cds()`, `candidate_partners()`, `selection_test()`. |
| `presence.py` | **Presence/absence retention matrix** → `PresenceReport` (member × species counts + retention calls; auto-flags lineage-specific retention). |
| `motifs.py` | **Family-signature PSSMs**: `derive_family_signatures()` → `FamilySignatureSet.scan()` — the domain-evidence fallback for candidates absent from UniProt/InterPro. |
| `esmfold.py` | **ESMFold segment fold-check** (`api.esmatlas.com`): mean pLDDT for a ≤400 aa segment — for IP3Rs, the C-terminal pore module. |
| `figures.py` | Publication figures (matplotlib): heatmap, NJ tree, conservation, length histogram, sources bar. |
| `pipeline.py` | `analyse(variants, …)` — the full pipeline; `write_analysis()` persists artefacts. |

## `src/discovery/` — novel-paralog hunter

| File | Key types / functions |
|------|-----------------------|
| `candidates.py` | `DiscoveryConfig`, `Candidate`, `DiscoveryReport`, `discover_novel_paralogs(…)`. Composite 8-criterion scorer + **promotion evidence gate** (D3: score ≥ 40 requires ≥ 1 family-specific component — outlier / domain / fold / split — else capped at 39) + the **sister-family test** (D14/D7, added in S1): a candidate closer to a labelled RyR bait than to any known ITPR paralog, by more than `sister_margin` (0.10), is assigned to the sister family and capped at 39, with the margin recorded. It is a positive test on distances — the candidate's own gene symbol is never consulted — and the length band is deliberately not part of the call. Without it all six RyR decoys were promoted at 45. Defaults read from `utils/family.py`. |
| `domain_scan.py` | `run_domain_scan(…)` — the fourth-paralog hunt: enumerate every protein with a family Pfam ID via InterPro, filter out known paralogs *and the ryanodine receptors*, fetch sequences, run analysis + discovery. |
| `exhaustive.py` | `run_exhaustive_hunt(…)` — the all-in-one `"mode": "exhaustive"` pipeline: Compara mine + ortholog expansion + InterPro enumeration → merged census (`itpr_like_census.csv` + FASTA) → analyse + discovery + presence matrix + highlighted tree + bundle/report. |

## `src/investigation/` — deep-dive on one candidate accession

| File | Key types / functions |
|------|-----------------------|
| `pipeline.py` | `Investigator(accession, options).run(on_progress)` → `InvestigationResult`; `InvestigationOptions` (panel defaults to human ITPR1/2/3 from `utils/family.py`). |
| `synthesis.py` | `assess_signals()`, `verdict_from_signals()`, `render_case_file()`. |
| `domain_arch.py` | Line 1 — full InterPro domain architecture. |
| `uniprot_detail.py` | Line 2 — detailed UniProt cross-refs + TM regions. |
| `structure.py` | Lines 3–4 — AlphaFold pLDDT + optional Foldseek. |
| `phylo_context.py` | Line 5 — panel MSA + NJ placement vs the known paralogs. |
| `synteny_lite.py` | Line 6 — genomic coordinates / neighbourhood. |
| `literature.py` | Line 7 — PubMed search, keyword from `utils/family.py`. |

## `src/utils/`

| File | Functions |
|------|-----------|
| `family.py` | The family definition (see above). |
| `species.py` | `SpeciesInfo`, `SPECIES_LOOKUP`, `DEFAULT_SPECIES_PANEL`, `resolve_species()`, `ensembl_species_slug()`. Covers the model organisms, the teleost 3R lineages and the non-metazoan margins. |
| `data_root.py` | `get_data_root()` (falls back to `<project>/data` with a warning), **`require_data_root()`** (raises — use before anything bulk), `free_bytes()`. `python -m src.utils.data_root --require` is the session-protocol check. |
| `exporters.py` | `write_fasta()`, `write_csv()`, `write_json()`. |
| `results_writer.py` | `make_bundle_dir()`, `write_bundle()`. |
| `report.py` | `write_report()` — publication-style markdown + standalone HTML with embedded figures. |

## `src/cli.py` — headless pipeline

`run_headless(project_root, preset, …, analyze, discover, interpro_lookup,
save_results)`: load preset → build `SearchQuery` (`"mode": "domain_scan"`
presets route to `run_domain_scan`) → `SearchOrchestrator` fans queries →
optional `analyse()` → optional `discover_novel_paralogs()` → optional
`write_bundle()` + `write_report()`.
`run_investigate(project_root, accession, …)` deep-dives one accession.

## Data Flow

```
SearchPanel.on_search ──▶ MainWindow._on_search ──▶ SearchOrchestrator.start
                                                          │
                                            ┌─────────────┼─────────────┐
                                            ▼             ▼             ▼
                                   NCBI / Ensembl / UniProt / Compara / AlphaFold
                                            │             │             │
                                            └────────┬────┴─────────────┘
                                                     ▼
                                          (sequence DBs done)
                                                     ▼
                                gather longest sequences ──▶ BLAST + Foldseek
                                                     ▼
                                                queue.Queue
                                                     ▼
                                  MainWindow._drain_queue (after())
                                                     ▼
                                              ResultsView.extend
                                                     ▼
                                                DetailsView.show
```

Post-search, the **Analysis menu** (`ToolsController` + `TaskRunner`) runs
`analyse()` / `discover_novel_paralogs()` / `run_domain_scan()` /
`Investigator` on a worker thread and shows results in `TextWindow`s.
